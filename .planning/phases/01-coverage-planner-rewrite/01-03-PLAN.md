---
phase: 01-coverage-planner-rewrite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ros2/src/mowgli_bringup/config/mowgli_robot.yaml
  - CLAUDE.md
autonomous: true
requirements: [R-6]
must_haves:
  truths:
    - "mowgli_robot.yaml has a robot_geometry: section with robot_length, robot_width, drive_axis_x_offset, drive_axis_y_offset, blade_x_offset, blade_y_offset, tool_width per D-07"
    - "Default values are documented as YardForce-500 empirical measurements per D-09 with caliper methodology comment"
    - "CLAUDE.md Architecture Invariant #7 is rewritten to describe the new deterministic full-path coverage_planner_node architecture (PlanCoverage.action, PlanCoverageGoal + FollowCoveragePlan BT nodes, WriteCheckpoint.srv-style key=value sidecars per D-05/D-06) — no stale references to ~/get_next_strip / GetNextStrip / find_next_unmowed_strip remain"
    - "CLAUDE.md Architecture Invariants section gains invariant #15 documenting the manual sync between mowgli_robot.yaml robot_geometry.robot_width and nav2_params.yaml collision_monitor.robot_width per D-08"
    - "CLAUDE.md `## ROS2 Specifics` `Coverage:` bullet and any other stale prose mentioning the deleted pull-path artifacts (GetNextUnmowedArea/GetNextStrip/TransitToStrip/FollowStrip/OutlineArea, ~/get_next_strip / ~/get_outline_path / ~/get_coverage_status, ensure_strip_layout / find_next_unmowed_strip / compute_outline_path) is rewritten to describe the new architecture"
  artifacts:
    - path: "ros2/src/mowgli_bringup/config/mowgli_robot.yaml"
      provides: "Robot geometry parameters consumed by coverage_planner_node"
      contains: "robot_geometry:"
    - path: "CLAUDE.md"
      provides: "Architecture invariants describe the new full-path coverage planner; documents new manual-sync invariant"
      contains: "robot_geometry"
  key_links:
    - from: "CLAUDE.md"
      to: "ros2/src/mowgli_bringup/config/mowgli_robot.yaml"
      via: "Architecture Invariant #15 text"
      pattern: "robot_geometry"
    - from: "CLAUDE.md"
      to: ".planning/phases/01-coverage-planner-rewrite/01-SPEC.md"
      via: "Architecture Invariant #7 text"
      pattern: "coverage_planner_node|PlanCoverage.action"
---

<objective>
Add the new `robot_geometry:` section to `mowgli_robot.yaml` (D-07), rewrite CLAUDE.md Architecture Invariant #7 to describe the new deterministic full-path coverage planner architecture (replacing the now-deleted pull-based strip planner described there), and add a new Architecture Invariant #15 documenting the manual-sync rule between `mowgli_robot.yaml:robot_geometry.robot_width` and `nav2_params.yaml:collision_monitor.robot_width` (D-08).

This plan is independent of the geometry library and the interface package — it only writes config + documentation.

Purpose: The coverage_planner_node (Plan 05) reads these parameters via standard ROS2 node-parameter loading. After Phase 1 ships, CLAUDE.md must accurately describe the live architecture: invariant #7 currently describes deleted code (Plan 06 removes the pull services + helper functions; Plan 08 removes the 5 BT coverage nodes), so leaving it untouched would violate the "live, accurate documentation" rule. The manual-sync invariant (#15) prevents silent drift between two sources of truth for `robot_width` (mowgli_robot.yaml vs. nav2_params.yaml collision_monitor).

Output: One YAML section added, two CLAUDE.md edits (rewrite of #7, append of #15, plus stale-prose scan-and-fix sweep over the rest of the file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-coverage-planner-rewrite/01-SPEC.md
@.planning/phases/01-coverage-planner-rewrite/01-CONTEXT.md
@.planning/phases/01-coverage-planner-rewrite/01-RESEARCH.md
@.planning/phases/01-coverage-planner-rewrite/01-PATTERNS.md
@CLAUDE.md
@ros2/src/mowgli_bringup/config/mowgli_robot.yaml
@ros2/src/mowgli_bringup/config/nav2_params.yaml

<interfaces>
D-07 (CONTEXT.md): Add new section robot_geometry: to existing mowgli_robot.yaml. Contains robot_length, robot_width, drive_axis_x_offset, drive_axis_y_offset, blade_x_offset, blade_y_offset, tool_width.
D-08 (CONTEXT.md): coverage_planner_node reads mowgli_robot.yaml; nav2_params.yaml collision_monitor section keeps its own robot_width: 0.40 — must be MANUALLY kept in sync. Document in CLAUDE.md "Architectural Invariants" section.
D-09 (CONTEXT.md): YardForce 500 empirical defaults: robot_length=0.60, robot_width=0.40, drive_axis_x_offset≈-0.20, blade_x_offset≈+0.25, blade_y_offset=0.0, tool_width=0.18.
PATTERNS.md §"Group 5: Config Changes" gives the exact YAML block to insert.

CLAUDE.md "Architectural Invariants (DO NOT VIOLATE)" section currently has 14 invariants. Invariant #7 today reads:
"Cell-based multi-area strip coverage — `map_server_node` plans strips on demand via `~/get_next_strip` service; no pre-planned full path. BT nodes `GetNextUnmowedArea` (outer loop, iterates through all mowing areas), `GetNextStrip` (inner loop, fetches strips for current area), `TransitToStrip`, `FollowStrip` execute sequentially. Progress tracked in `mow_progress` grid layer (survives restarts). Coverage status via `~/get_coverage_status` service and `/map_server_node/coverage_cells` OccupancyGrid topic."

That description contradicts what Phase 1 ships:
- Plan 06 deletes `~/get_next_strip`, `~/get_outline_path`, `~/get_coverage_status` and the helper functions `ensure_strip_layout`, `find_next_unmowed_strip`, `compute_outline_path`.
- Plan 08 deletes the BT nodes `GetNextStrip`, `FollowStrip`, `TransitToStrip`, `OutlineArea`, `GetNextUnmowedArea`.
- Plan 05 introduces `coverage_planner_node` with `PlanCoverage.action`.
- Plan 08 introduces the new BT nodes `PlanCoverageGoal` (one-shot at COMMAND_START) and `FollowCoveragePlan` (sequential `CoverageWaypoint[]` follower).
- Per-area `coverage_<area_index>.kv` checkpoints replace the `mow_progress` grid layer (D-05/D-06 + SPEC R-10).

Therefore #7 must be rewritten (not extended); invariant #15 is a new entry appended after #14. The other invariants (1-6, 8-14) describe localization, TF, DDS, dock detection, undock, etc. — they remain valid.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add robot_geometry: section to mowgli_robot.yaml</name>
  <read_first>
    - ros2/src/mowgli_bringup/config/mowgli_robot.yaml (current file in full — understand existing top-level keys, indent, comment style, presence of tool_width / blade_radius / undock_* parameters)
    - ros2/src/mowgli_bringup/config/nav2_params.yaml (search for "collision_monitor" — confirm robot_width: 0.40 exists there so the invariant text in Task 2 references real values)
    - .planning/phases/01-coverage-planner-rewrite/01-PATTERNS.md §"Group 5: Config Changes" (exact YAML block + comment block)
  </read_first>
  <files>
    ros2/src/mowgli_bringup/config/mowgli_robot.yaml
  </files>
  <action>
Open ros2/src/mowgli_bringup/config/mowgli_robot.yaml. The file uses ROS2 standard parameter-file shape: top-level node name, then `ros__parameters:` then leaf params at 2-space indent.

Determine the existing top-level node name. The new `robot_geometry:` block must be inserted under the same `ros__parameters:` section the coverage_planner_node will load. If there is an existing top-level grouping that already holds tool_width / blade_radius / undock_distance / dock_approach_distance, append `robot_geometry:` as a sub-map at the same indent depth as those siblings (NOT as a new top-level node block).

Insert this block (preserve the file's existing leading indent — match the indent of existing siblings like `tool_width:` and `blade_radius:`; the example below uses 4-space indent, adjust to match the actual file):
```yaml
    # -----------------------------------------------------------------
    # Robot footprint geometry (used by coverage_planner_node, Phase 1)
    # Measured at hardware bench - caliper from drive axle centerline.
    # Methodology: place robot on flat surface; measure with caliper or
    # baseline tape:
    #   robot_length        = bumper-to-bumper distance, front-to-back
    #   robot_width         = chassis width at the widest point
    #   drive_axis_x_offset = drive-axle distance from chassis center, X
    #                         (negative = axle behind chassis center;
    #                          YardForce 500 has axle at the rear, so
    #                          dx is approximately -0.20)
    #   drive_axis_y_offset = lateral axle offset, normally 0 for
    #                         symmetric chassis
    #   blade_x_offset      = blade-disc center distance from chassis
    #                         center, X (YardForce 500: blade on front
    #                         half, approximately +0.25)
    #   blade_y_offset      = lateral blade offset (normally 0)
    #   tool_width          = effective blade cut width
    #
    # IMPORTANT (Architecture Invariant #15, see CLAUDE.md):
    # robot_width here MUST match collision_monitor.robot_width in
    # nav2_params.yaml. There is no automated sync - change both files
    # in the same commit.
    # -----------------------------------------------------------------
    robot_geometry:
      robot_length: 0.60
      robot_width: 0.40
      drive_axis_x_offset: -0.20
      drive_axis_y_offset: 0.0
      blade_x_offset: 0.25
      blade_y_offset: 0.0
      tool_width: 0.18
```

Notes:
- If a top-level `tool_width:` parameter already exists in mowgli_robot.yaml at the same scope, leave it AS-IS for now. coverage_planner_node will read `robot_geometry.tool_width`; other existing consumers continue reading the top-level `tool_width:` until they migrate. Operator must commit both with the same value.
- Insert the block AFTER existing tool_width/blade_radius siblings to keep related params co-located.
- Do not change any other line in the file.
- Match YAML 2-space-per-level indent and snake_case keys (per .claude/rules/ros2.md "Code Style").
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('ros2/src/mowgli_bringup/config/mowgli_robot.yaml')); print('YAML parsed OK')" && grep -c "robot_geometry:\|drive_axis_x_offset:\|drive_axis_y_offset:\|blade_x_offset:\|blade_y_offset:" ros2/src/mowgli_bringup/config/mowgli_robot.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('ros2/src/mowgli_bringup/config/mowgli_robot.yaml'))"` exits 0 (file is still valid YAML).
    - `grep -q "robot_geometry:" ros2/src/mowgli_bringup/config/mowgli_robot.yaml` succeeds.
    - `grep -c "drive_axis_x_offset:\|drive_axis_y_offset:\|blade_x_offset:\|blade_y_offset:" ros2/src/mowgli_bringup/config/mowgli_robot.yaml` returns at least 4 (one for each new key).
    - `grep -q "Architecture Invariant #15" ros2/src/mowgli_bringup/config/mowgli_robot.yaml` succeeds (cross-reference to CLAUDE.md present).
    - `grep -c "0.60\|0.40\|-0.20\|0.25\|0.18" ros2/src/mowgli_bringup/config/mowgli_robot.yaml` returns at least 5 (default values present, per D-09).
    - `colcon build --packages-select mowgli_bringup --event-handlers console_cohesion+` exits 0 (config-only package still builds).
  </acceptance_criteria>
  <done>YAML parses cleanly; 7 robot_geometry leaf keys present with YardForce 500 defaults; methodology + invariant comment present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite CLAUDE.md Architecture Invariant #7, add #15, scrub stale pull-path prose</name>
  <read_first>
    - CLAUDE.md (full file — understand exact format of "Architectural Invariants (DO NOT VIOLATE)" section, find current invariants 1-14, find the line right after #14, AND scan the rest of the file for stale prose listed in the "Stale-prose scan list" below)
    - .planning/phases/01-coverage-planner-rewrite/01-SPEC.md (full file — needed for the new architecture vocabulary: `coverage_planner_node`, `PlanCoverage.action`, `CoverageWaypoint`, segment_type enum values, validation pipeline, sparse plan)
    - .planning/phases/01-coverage-planner-rewrite/01-CONTEXT.md (D-01 .. D-12 — D-03 = `FollowCoveragePlan` BT node, D-04 = `PlanCoverageGoal` BT node, D-05/D-06 = `coverage_<area_index>.kv` sidecar checkpoint format, D-08 = manual-sync rule for invariant #15)
    - The "Suggested replacement text for invariant #7" provided in the revision message (you may copy verbatim or refine the wording, but the result MUST contain the strings `coverage_planner_node`, `PlanCoverage.action`, and reference checkpoint persistence; the result MUST NOT contain `~/get_next_strip`, `GetNextStrip`, `find_next_unmowed_strip`, `ensure_strip_layout`, `compute_outline_path`)
  </read_first>
  <files>
    CLAUDE.md
  </files>
  <action>
Open CLAUDE.md. This task has THREE sub-steps — do all three in one edit pass.

---

**Sub-step 2A: Rewrite Architecture Invariant #7 (replace, do not extend)**

Locate the section heading `## Architecture Invariants (DO NOT VIOLATE)` and find invariant #7. Today it reads (verbatim, for diff/regression purposes):

```
7. **Cell-based multi-area strip coverage** — `map_server_node` plans strips on demand via `~/get_next_strip` service; no pre-planned full path. BT nodes `GetNextUnmowedArea` (outer loop, iterates through all mowing areas), `GetNextStrip` (inner loop, fetches strips for current area), `TransitToStrip`, `FollowStrip` execute sequentially. Progress tracked in `mow_progress` grid layer (survives restarts). Coverage status via `~/get_coverage_status` service and `/map_server_node/coverage_cells` OccupancyGrid topic.
```

REPLACE the entire #7 paragraph with the new text below (or a refinement of it that preserves the listed required strings and absent strings):

```markdown
7. **Deterministic full-path coverage planner.** `coverage_planner_node` (package `mowgli_coverage_planner`) pre-plans the complete sequential `PoseStamped[]` waypoint list for every working area + obstacle outline + boustrophedon swath via `PlanCoverage.action` before any blade rotates. The plan is sparse (one pose per outline vertex / swath endpoint / dock waypoint; Nav2 `FollowPath` densifies during execution) and every waypoint carries a `segment_type` ∈ {UNDOCK, TRANSIT, OUTLINE_WORKING_AREA, OUTLINE_OBSTACLE, MOWING_BOUSTROPHEDON, RETURN_TO_DOCK, DOCK_APPROACH, DOCKING}. The BT node `PlanCoverageGoal` runs once at COMMAND_START, sends the Action goal, and writes the resulting plan to the BT blackboard. The monolithic `FollowCoveragePlan` `BT.CPP v4 StatefulActionNode` then walks the plan sequentially, dispatching each waypoint to Nav2 `NavigateToPose` (UNDOCK / TRANSIT / DOCK_APPROACH / RETURN_TO_DOCK / DOCKING via RPP) or Nav2 `FollowPath` (OUTLINE_* and MOWING_BOUSTROPHEDON via FTCController) and toggling the blade per-segment via `mowgli_interfaces/srv/MowerControl`. Progress is checkpointed per area to `<areas_dir>/coverage_<area_index>.kv` sidecars (`key=value` text, one field per line — yaml-cpp is intentionally avoided per `hardware_bridge_node.cpp:99`) written atomically via tempfile + `rename(2)`; resume re-issues the Action goal with `resume_from_checkpoint = true` and continues at the open swath endpoint within ≤ 5 cm + 5° yaw of the persisted state. Collision avoidance during execution is `collision_monitor` + Nav2's local planner only — there is no global re-planning on dynamic obstacles, no continuous re-trigger on area edits (the plan is a snapshot per Action goal). `map_server_node` no longer plans paths; it serves area geometry via `GetAllAreas.srv` and persists the area DB. Pre-flight validation runs all 10 spec safety points before plan emission; failure returns a typed `PlanError` instead of a partial plan.
```

This must remain numbered #7. Do NOT renumber #8-#14. Do NOT change any other invariant 1-6 or 8-14 (those describe localization, TF, DDS, frames, dock detection, undock behavior, costmap config, grid_map convention — all still valid).

---

**Sub-step 2B: Append new Architecture Invariant #15 (manual-sync rule)**

Immediately after invariant #14 (Coverage grid_map → OccupancyGrid convention), and before the next top-level `##` heading (`## High-Level Commands and States`), insert:

```markdown
15. **Robot footprint geometry sync.** `mowgli_robot.yaml` `robot_geometry.robot_width` MUST equal `nav2_params.yaml` `collision_monitor:` `robot_width`. The two files are independent sources of truth for the same physical dimension — `coverage_planner_node` reads the former for footprint-aware geometry checks (SPEC R-6, Phase 1), `collision_monitor` reads the latter at runtime for live obstacle checks. There is no automated sync (collision_monitor has no native footprint-topic subscribe); changing one without the other silently breaks safety guarantees. **Always commit changes to both files in the same commit.** Default values (YardForce 500): `robot_length: 0.60`, `robot_width: 0.40`, `drive_axis_x_offset: -0.20`, `blade_x_offset: 0.25`, `tool_width: 0.18`.
```

---

**Sub-step 2C: Stale-prose scan + fix sweep**

After the invariant edits, scan the rest of CLAUDE.md (especially the `## ROS2 Specifics` section and the `## What NOT to Do` section) for any other prose that references the deleted pull-path artifacts. The scan list is:

- `~/get_next_strip`
- `~/get_outline_path`
- `~/get_coverage_status`
- `/map_server_node/coverage_cells`
- `GetNextStrip`
- `GetNextUnmowedArea`
- `TransitToStrip`
- `FollowStrip`
- `OutlineArea` (only when used as a BT node name; the literal phrase "outline area" in geometry-prose is fine)
- `compute_outline_path`
- `find_next_unmowed_strip`
- `ensure_strip_layout`
- `is_strip_mowed`
- `is_strip_blocked`
- `mow_progress` grid layer (when used as the SOURCE OF TRUTH for coverage progress — note that the `mow_progress.png` file may still be referenced as a render artifact; only references implying it tracks semantic coverage state should be updated)
- The phrase "cell-based strip coverage" / "Cell-based multi-area strip coverage" / "no pre-planned full path"
- The phrase "no full-path pre-planning"

For each hit, decide:
1. **In `## ROS2 Specifics` `Coverage:` bullet (line ~112 today)** — the current bullet describes the old pull architecture. Rewrite to a one-sentence pointer to invariant #7, e.g.:
   ```markdown
   - **Coverage:** Deterministic full-path planner in `coverage_planner_node` (Architecture Invariant #7). `PlanCoverage.action` returns a sparse `CoverageWaypoint[]` covering all working areas + obstacle outlines + return-to-dock; BT walks the plan sequentially via `FollowCoveragePlan`. Per-area `coverage_<area_index>.kv` sidecars track progress for charge-cycle resume. `map_server_node` no longer plans paths; it serves area geometry via `GetAllAreas.srv` only.
   ```
2. **The first-paragraph project description (line 3 today)** — currently says "BehaviorTree.CPP v4, cell-based strip coverage." Replace "cell-based strip coverage" with "BehaviorTree.CPP v4, full-path coverage planner." (Keep the line short.)
3. **The `## What NOT to Do` section** — currently DOES NOT reference any of the deleted artifacts directly (the existing "Do NOT use MPPI controller for coverage paths" and "Do NOT use RPP for coverage paths" bullets remain valid). Append ONE new bullet after the existing list:
   ```markdown
   - Do NOT change `mowgli_robot.yaml:robot_geometry.robot_width` or `nav2_params.yaml:collision_monitor:robot_width` independently — the two are manually-synced sources of truth (Architecture Invariant #15). Always change both in the same commit with the same value.
   ```
4. **Anywhere else** — if any other prose in the file mentions the scan-list items in a way that implies the deleted code still exists, rewrite to match the new architecture. If you find an unanticipated reference, rewrite minimally (one sentence) and note it in the SUMMARY.md.

If a scan-list item appears INSIDE invariant #7 itself, it belongs to the OLD verbatim-quote text — the rewrite in Sub-step 2A handles that. Do not double-edit.

---

**Style requirements (apply to all edits in this task):**
- Match the existing markdown style: numbered, bold-prefix label, period-terminated declarative sentences for invariants; bulleted prose for the ROS2 Specifics list.
- Do NOT renumber existing invariants.
- Do NOT add Co-Authored-By lines (per CLAUDE.md commit conventions).
- Do NOT add emojis.
- Keep prose concise and outcome-shaped, matching neighbor invariants in tone.
  </action>
  <verify>
    <automated>
# Sub-step 2A: invariant #7 contains required new strings
grep -q "^7\. \*\*Deterministic full-path coverage planner" CLAUDE.md && \
grep -q "coverage_planner_node" CLAUDE.md && \
grep -q "PlanCoverage\.action" CLAUDE.md && \
grep -q "PlanCoverageGoal" CLAUDE.md && \
grep -q "FollowCoveragePlan" CLAUDE.md && \
grep -q "coverage_<area_index>\.kv\|coverage_<area_index>.kv" CLAUDE.md && \
\
# Sub-step 2A regression: invariant #7 NO LONGER contains stale strings
! grep -q "~/get_next_strip" CLAUDE.md && \
! grep -q "~/get_outline_path" CLAUDE.md && \
! grep -q "~/get_coverage_status" CLAUDE.md && \
! grep -q "GetNextStrip" CLAUDE.md && \
! grep -q "GetNextUnmowedArea" CLAUDE.md && \
! grep -q "TransitToStrip" CLAUDE.md && \
! grep -q "find_next_unmowed_strip" CLAUDE.md && \
! grep -q "ensure_strip_layout" CLAUDE.md && \
! grep -q "compute_outline_path" CLAUDE.md && \
! grep -q "Cell-based multi-area strip coverage" CLAUDE.md && \
\
# Sub-step 2B: invariant #15 present
grep -q "^15\. \*\*Robot footprint geometry sync" CLAUDE.md && \
grep -q "robot_geometry\.robot_width" CLAUDE.md && \
grep -q "collision_monitor" CLAUDE.md && \
\
# Sub-step 2C: What NOT to Do bullet for invariant #15 sync rule
grep -Eq "Do NOT change.*robot_geometry\.robot_width.*independently" CLAUDE.md && \
\
# Other invariants 1-6, 8-14 still present (key-phrase regression check on a few)
grep -q "^1\. \*\*robot_localization" CLAUDE.md && \
grep -q "^2\. \*\*TF chain follows REP-105" CLAUDE.md && \
grep -q "^3\. \*\*Cyclone DDS" CLAUDE.md && \
grep -q "^8\. \*\*FTCController for coverage paths" CLAUDE.md && \
grep -q "^14\. \*\*Coverage grid_map" CLAUDE.md && \
\
# Cross-reference to invariant #15 appears in at least 2 locations
test "$(grep -c "Architecture Invariant #15" CLAUDE.md)" -ge 2 && \
echo "All CLAUDE.md verification checks passed"
    </automated>
  </verify>
  <acceptance_criteria>
    - **Sub-step 2A (invariant #7 rewrite, claude_md_compliance blocker fix):**
      - `grep -q "^7\. \*\*Deterministic full-path coverage planner" CLAUDE.md` succeeds (new invariant header present).
      - `grep -q "coverage_planner_node" CLAUDE.md` succeeds.
      - `grep -q "PlanCoverage\.action" CLAUDE.md` succeeds.
      - `grep -q "PlanCoverageGoal" CLAUDE.md` AND `grep -q "FollowCoveragePlan" CLAUDE.md` both succeed (new BT node names referenced).
      - `grep -qE "coverage_<area_index>\.kv|coverage_[0-9]+\.kv|coverage_.*\.kv" CLAUDE.md` succeeds (checkpoint sidecar format referenced).
      - **Regression check (this is the crucial part of the rewrite):**
        - `! grep -q "~/get_next_strip" CLAUDE.md` (deleted pull service).
        - `! grep -q "~/get_outline_path" CLAUDE.md`.
        - `! grep -q "~/get_coverage_status" CLAUDE.md`.
        - `! grep -q "GetNextStrip" CLAUDE.md` AND `! grep -q "GetNextUnmowedArea" CLAUDE.md` AND `! grep -q "TransitToStrip" CLAUDE.md` (deleted BT nodes — note: this is checked file-wide because Sub-step 2C also removes any other occurrence).
        - `! grep -q "find_next_unmowed_strip" CLAUDE.md`.
        - `! grep -q "ensure_strip_layout" CLAUDE.md`.
        - `! grep -q "compute_outline_path" CLAUDE.md`.
        - `! grep -q "Cell-based multi-area strip coverage" CLAUDE.md` (the OLD invariant #7 header).
    - **Sub-step 2B (invariant #15 add):**
      - `grep -q "^15\. \*\*Robot footprint geometry sync" CLAUDE.md` succeeds.
      - `grep -q "robot_geometry\.robot_width" CLAUDE.md` succeeds.
      - `grep -q "collision_monitor" CLAUDE.md` succeeds.
      - `grep -c "^[0-9]\+\. \*\*" CLAUDE.md` returns 15 (one numbered invariant per line, 1..15) — Note: only counts top-level numbered invariant headings, not list items elsewhere.
    - **Sub-step 2C (stale-prose scrub):**
      - `## ROS2 Specifics` `Coverage:` bullet now references `coverage_planner_node` and `FollowCoveragePlan`, not the old pull artifacts: `grep -A 1 "^- \*\*Coverage:\*\*" CLAUDE.md | grep -qE "coverage_planner_node|FollowCoveragePlan"`.
      - The first-paragraph project description on line 3 no longer says "cell-based strip coverage": `! grep -q "cell-based strip coverage" CLAUDE.md` (case-sensitive — the OLD wording).
      - `## What NOT to Do` section gains a new bullet: `grep -Eq "Do NOT change.*robot_geometry\.robot_width.*independently" CLAUDE.md` succeeds.
      - At least 2 occurrences of "Architecture Invariant #15" appear file-wide (one in #15 itself optional, one in YAML cross-reference from Task 1, one in the new "Do NOT" bullet — at least 2 of these).
    - **Other invariants 1-6, 8-14 unchanged (regression):**
      - `grep -q "^1\. \*\*robot_localization" CLAUDE.md` succeeds.
      - `grep -q "^2\. \*\*TF chain follows REP-105" CLAUDE.md` succeeds.
      - `grep -q "^3\. \*\*Cyclone DDS" CLAUDE.md` succeeds.
      - `grep -q "^4\. \*\*Map frame = GPS frame" CLAUDE.md` succeeds.
      - `grep -q "^5\. \*\*Costmap obstacles disabled" CLAUDE.md` succeeds.
      - `grep -q "^6\. \*\*dock_pose_yaw auto-captured" CLAUDE.md` succeeds.
      - `grep -q "^8\. \*\*FTCController for coverage paths" CLAUDE.md` succeeds.
      - `grep -q "^9\. \*\*Emergency auto-reset on dock" CLAUDE.md` succeeds.
      - `grep -q "^10\. \*\*Undock via Nav2 BackUp behavior" CLAUDE.md` succeeds.
      - `grep -q "^11\. \*\*Zero-odom only when charging" CLAUDE.md` succeeds.
      - `grep -q "^12\. \*\*Battery current for dock detection" CLAUDE.md` succeeds.
      - `grep -q "^13\. \*\*Docking server cmd_vel" CLAUDE.md` succeeds.
      - `grep -q "^14\. \*\*Coverage grid_map" CLAUDE.md` succeeds.
  </acceptance_criteria>
  <done>
CLAUDE.md invariant #7 is rewritten to describe the new deterministic full-path coverage planner architecture and contains zero references to deleted pull-path artifacts; invariant #15 added documenting the robot_width manual-sync rule; ROS2 Specifics `Coverage:` bullet rewritten; first-paragraph "cell-based strip coverage" phrase rewritten; What NOT to Do gains the cross-reference bullet; existing invariants 1-6 and 8-14 untouched.
  </done>
</task>

</tasks>

<threat_model>
No new attack surface. This plan only writes config defaults and documentation. The robot_geometry parameters become an attacker-relevant surface only when consumed by coverage_planner_node (Plan 05) — that's where validation (`robot_length > 0`, `robot_width > 0`, `tool_width > 0` per D-09) lives. This plan introduces neither network input, nor filesystem write logic, nor parsing code.

The CLAUDE.md edits are pure documentation. The only "safety-relevant" aspect is correctness: if invariant #7 is left describing deleted code, future Claude sessions will plan against the wrong architecture — that's the bug this revision fixes.
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('ros2/src/mowgli_bringup/config/mowgli_robot.yaml'))"` exits 0
- `colcon build --packages-select mowgli_bringup` exits 0
- CLAUDE.md invariant #7 rewritten — `grep` for `coverage_planner_node` succeeds, `grep` for `~/get_next_strip` / `GetNextStrip` / `find_next_unmowed_strip` / `ensure_strip_layout` / `compute_outline_path` ALL fail file-wide
- CLAUDE.md invariant #15 present as a numbered entry with the manual-sync rule
- `## What NOT to Do` bullet added cross-referencing #15
- `## ROS2 Specifics` `Coverage:` bullet describes the new architecture (references `coverage_planner_node`, `FollowCoveragePlan`)
- First-paragraph project description no longer says "cell-based strip coverage"
- All invariants 1-6 and 8-14 unchanged (`grep` regression checks pass)
</verification>

<success_criteria>
- D-07 realized: robot_geometry: section in mowgli_robot.yaml with 7 leaf keys + methodology comment.
- D-08 realized: CLAUDE.md invariant #15 documents the manual-sync rule between mowgli_robot.yaml and nav2_params.yaml; What-NOT-to-Do bullet added.
- D-09 realized: empirical YardForce 500 default values committed.
- Checker blocker (claude_md_compliance) closed: invariant #7 now describes the new full-path coverage planner architecture; no stale references to ~/get_next_strip / GetNextStrip / find_next_unmowed_strip remain anywhere in CLAUDE.md.
- Checker warning (dependency_correctness) closed: plan's `wave: 1` matches its empty `depends_on`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coverage-planner-rewrite/01-03-SUMMARY.md` describing:
1. The YAML block added to `mowgli_robot.yaml` (with the 7 robot_geometry keys + methodology comment).
2. The new CLAUDE.md Architecture Invariant #7 wording (verbatim, so future plans can verify).
3. The new CLAUDE.md Architecture Invariant #15 wording (verbatim).
4. The exact set of stale-prose hits found during the Sub-step 2C scan and what was rewritten (one bullet per hit, with old text → new text).
5. The new "What NOT to Do" bullet (verbatim).
6. Confirmation that nav2_params.yaml's existing `collision_monitor.robot_width` is currently 0.40 (matching the new robot_geometry.robot_width default).
7. Confirmation that all CLAUDE.md regression-check `grep`s passed (invariants 1-6 and 8-14 unchanged).
</output>
