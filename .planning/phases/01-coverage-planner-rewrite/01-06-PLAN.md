---
phase: 01-coverage-planner-rewrite
plan: 06
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - ros2/src/mowgli_map/src/map_server_node.cpp
  - ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp
  - ros2/src/mowgli_map/CMakeLists.txt
  - ros2/src/mowgli_map/package.xml
  - ros2/src/mowgli_interfaces/srv/GetNextStrip.srv
  - ros2/src/mowgli_interfaces/srv/GetCoverageStatus.srv
  - ros2/src/mowgli_interfaces/srv/PreviewPlan.srv
  - ros2/src/mowgli_interfaces/srv/GetOutlinePath.srv
  - ros2/src/mowgli_interfaces/CMakeLists.txt
autonomous: true
requirements: [R-1]
must_haves:
  truths:
    - "map_server_node hosts the new /map_server_node/get_all_areas service serving MapArea[] including narrow_area_strategy"
    - "Pull-path artifacts are deleted from map_server_node: ensure_strip_layout, find_next_unmowed_strip, strip_to_path, on_get_next_strip, on_get_coverage_status, on_preview_plan, on_get_outline_path, compute_outline_path"
    - "Geometry helpers (offset_polygon_inward, convex_hull, point_in_polygon, compute_optimal_mow_angle) are removed from map_server_node and consumed via #include <mowgli_geometry/geometry.hpp> in the call sites that still need them"
    - "Pull-path service definitions (.srv files) are deleted from mowgli_interfaces and removed from CMakeLists.txt: GetNextStrip.srv, GetCoverageStatus.srv, PreviewPlan.srv, GetOutlinePath.srv"
    - "map_server_node loads narrow_area_strategy on areas.yaml read and persists it on areas.yaml write (range 0-2 enforced; out-of-range -> coerced to 0=SKIP with WARN log)"
    - "colcon build --packages-select mowgli_interfaces mowgli_map exits 0"
  artifacts:
    - path: "ros2/src/mowgli_map/src/map_server_node.cpp"
      provides: "GetAllAreas service handler + cleanup of strip planner code"
      contains: "on_get_all_areas"
    - path: "ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp"
      provides: "Updated public surface"
      contains: "on_get_all_areas"
  key_links:
    - from: "ros2/src/mowgli_map/src/map_server_node.cpp"
      to: "mowgli_geometry"
      via: "#include + ament_target_dependencies"
      pattern: "mowgli_geometry"
    - from: "ros2/src/mowgli_map/CMakeLists.txt"
      to: "mowgli_geometry"
      via: "find_package + ament_target_dependencies"
      pattern: "find_package(mowgli_geometry"
---

<objective>
Replace `map_server_node`'s legacy pull-based coverage code with the new `GetAllAreas` service handler. Migrate any remaining geometry callers to `mowgli_geometry`. Delete the four pull-path service definitions from `mowgli_interfaces`. Persist `narrow_area_strategy` through the existing areas.yaml round-trip.

Purpose: Per CONTEXT.md "single source of truth (Q1.1=a) over parallel-keep" — pull-path artifacts are deleted in this phase, not deprecated. This plan executes that cleanup. Plan 05 already wired coverage_planner_node to call /map_server_node/get_all_areas, so this plan is a runtime prerequisite for the planner to function.
Output: map_server_node.cpp shrinks by 8+ functions and the four .srv files are gone. New GetAllAreas service handler is live; areas.yaml carries the new field; existing tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-coverage-planner-rewrite/01-SPEC.md
@.planning/phases/01-coverage-planner-rewrite/01-CONTEXT.md
@.planning/phases/01-coverage-planner-rewrite/01-RESEARCH.md
@.planning/phases/01-coverage-planner-rewrite/01-PATTERNS.md
@CLAUDE.md
@.claude/rules/ros2.md

<interfaces>
PATTERNS.md §"Group 7: map_server_node Cleanup" lists the exact function names to delete:
- ensure_strip_layout() (line 2504)
- find_next_unmowed_strip()
- strip_to_path()
- on_get_next_strip() service handler
- on_get_coverage_status() service handler
- on_preview_plan() service handler
- on_get_outline_path() service handler
- compute_outline_path() (line 3420 — moved logic-wise; only the caller-facing wrapper is removed if its remaining call sites are also removed)
- offset_polygon_inward() (line 3298)
- convex_hull() (line 2421)
- compute_optimal_mow_angle() (line 2459)
- point_in_polygon() (line 1389)

Plus state members in map_server_node.hpp:
- std::vector<StripLayout> strip_layouts_;
- std::vector<int> current_strip_idx_;
(any other strip_* state — discover via grep)

GetAllAreas handler from PATTERNS.md §"Group 7":
```cpp
void MapServerNode::on_get_all_areas(
    const mowgli_interfaces::srv::GetAllAreas::Request::SharedPtr /*req*/,
    mowgli_interfaces::srv::GetAllAreas::Response::SharedPtr res)
{
  std::lock_guard<std::mutex> lock(map_mutex_);
  res->areas.reserve(areas_.size());
  for (const auto& a : areas_) {
    mowgli_interfaces::msg::MapArea msg;
    msg.name = a.name;
    msg.area = a.polygon;
    msg.obstacles = a.obstacles;
    msg.is_navigation_area = a.is_navigation_area;
    msg.narrow_area_strategy = a.narrow_area_strategy;  // new field per SPEC R-13
    res->areas.push_back(msg);
  }
}
```

The internal `MowingArea` struct in map_server_node.hpp must gain `uint8_t narrow_area_strategy{0}` to mirror MapArea.narrow_area_strategy.

areas.yaml persistence (existing custom non-yaml-cpp parser/writer pair `save_areas_to_file` / `load_areas_from_file` — follow the existing convention exactly; add narrow_area_strategy as a new field with default 0 when absent for forward-compat with existing on-disk areas.yaml files).

Removed services in mowgli_interfaces/CMakeLists.txt (the `set(srv_files ...)` block from Plan 01) — entries to DELETE:
- "srv/GetNextStrip.srv"
- "srv/GetCoverageStatus.srv"
- "srv/PreviewPlan.srv"
- "srv/GetOutlinePath.srv"
The .srv files themselves are deleted from disk in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add GetAllAreas service handler + extend MowingArea/areas.yaml round-trip with narrow_area_strategy</name>
  <read_first>
    - ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp (full file — find the MowingArea struct definition; identify the strip-planner state members in lines 491-494 per PATTERNS.md)
    - ros2/src/mowgli_map/src/map_server_node.cpp (locate save_areas_to_file and load_areas_from_file via grep; understand the on_get_mowing_area handler as the closest analog for on_get_all_areas; locate the constructor's create_service<...> block to add the new service)
    - ros2/src/mowgli_interfaces/srv/GetMowingArea.srv (existing single-area handler analog)
    - install/mowgli_interfaces/include/mowgli_interfaces/srv/get_all_areas.hpp (generated by Plan 01 — confirm Request/Response shape)
    - .planning/phases/01-coverage-planner-rewrite/01-PATTERNS.md §"Group 7: map_server_node Cleanup"
  </read_first>
  <files>
    ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp (MODIFY — add MowingArea.narrow_area_strategy + on_get_all_areas decl + new srv member)
    ros2/src/mowgli_map/src/map_server_node.cpp (MODIFY — add on_get_all_areas + areas.yaml round-trip + service registration)
    ros2/src/mowgli_map/CMakeLists.txt (MODIFY — find_package(mowgli_geometry REQUIRED) + ament_target_dependencies(... mowgli_geometry))
    ros2/src/mowgli_map/package.xml (MODIFY — <depend>mowgli_geometry</depend>)
  </files>
  <action>
**Step 1 — package.xml**: add `<depend>mowgli_geometry</depend>` to the runtime deps list (place it next to the existing `<depend>mowgli_interfaces</depend>`).

**Step 2 — CMakeLists.txt**: add `find_package(mowgli_geometry REQUIRED)` next to existing `find_package(mowgli_interfaces REQUIRED)`. In the existing `ament_target_dependencies(... mowgli_interfaces ...)` block, add `mowgli_geometry`. (Header-only INTERFACE target propagates Eigen3+Boost.)

**Step 3 — map_server_node.hpp**:
- Inside the internal `MowingArea` struct (the C++ struct, not the ROS msg), add:
  ```cpp
  uint8_t narrow_area_strategy{0};   // 0=SKIP, 1=OUTLINE_ONLY, 2=SPECIAL_PATTERN per SPEC R-13
  ```
  Place it next to `bool is_navigation_area;`.
- Add the new service member:
  ```cpp
  rclcpp::Service<mowgli_interfaces::srv::GetAllAreas>::SharedPtr get_all_areas_srv_;
  ```
- Add the new handler decl:
  ```cpp
  void on_get_all_areas(
      const mowgli_interfaces::srv::GetAllAreas::Request::SharedPtr req,
      mowgli_interfaces::srv::GetAllAreas::Response::SharedPtr res);
  ```
- Add the include at the top: `#include <mowgli_interfaces/srv/get_all_areas.hpp>`.

**Step 4 — map_server_node.cpp**:
- Add the include: `#include <mowgli_interfaces/srv/get_all_areas.hpp>`.
- In the constructor, register the service immediately after the existing `get_mowing_area` service registration:
  ```cpp
  get_all_areas_srv_ = create_service<mowgli_interfaces::srv::GetAllAreas>(
      "~/get_all_areas",
      std::bind(&MapServerNode::on_get_all_areas, this,
                std::placeholders::_1, std::placeholders::_2));
  ```
  (Relative name `~/get_all_areas` resolves to `/map_server_node/get_all_areas` because the node is named `map_server_node` — matches Plan 05's expectation.)
- Implement `on_get_all_areas` per PATTERNS.md §"Group 7" — copy the snippet verbatim and adapt to the actual lock/mutex name in the existing file.

**Step 5 — areas.yaml round-trip**:
Locate `save_areas_to_file` (the writer) and `load_areas_from_file` (the reader) in map_server_node.cpp.

Writer change: add a single new line per area, e.g. `narrow_area_strategy: <int>`. Position: after the existing `is_navigation_area` line. Use the same indentation/format the existing writer uses.

Reader change: when parsing each area block, look for the new field. If present, parse as integer and clamp to 0..2. If absent (legacy areas.yaml from before this phase), default to 0 (SKIP) for forward-compat. If a value outside 0..2 is read, log RCLCPP_WARN and coerce to 0.
  </action>
  <verify>
    <automated>cd ros2 && colcon build --packages-select mowgli_map --event-handlers console_cohesion+ 2>&1 | tail -25 && colcon test --packages-select mowgli_map --event-handlers console_cohesion+ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "<depend>mowgli_geometry</depend>" ros2/src/mowgli_map/package.xml` succeeds.
    - `grep -q "find_package(mowgli_geometry REQUIRED)" ros2/src/mowgli_map/CMakeLists.txt` succeeds.
    - `grep -q "uint8_t narrow_area_strategy" ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp` succeeds.
    - `grep -q "on_get_all_areas" ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp` succeeds.
    - `grep -q "create_service<mowgli_interfaces::srv::GetAllAreas>" ros2/src/mowgli_map/src/map_server_node.cpp` succeeds.
    - `grep -q "narrow_area_strategy" ros2/src/mowgli_map/src/map_server_node.cpp` returns at least 3 occurrences (struct copy in handler + writer + reader).
    - `colcon build --packages-select mowgli_map` exits 0.
    - Existing map_server unit tests still pass: `colcon test --packages-select mowgli_map` reports 0 failures (existing tests do not exercise narrow_area_strategy yet, but they must not regress).
  </acceptance_criteria>
  <done>map_server_node hosts /map_server_node/get_all_areas; areas.yaml round-trips narrow_area_strategy; mowgli_geometry is a real dependency.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete pull-path services + delete strip-planner code from map_server_node</name>
  <read_first>
    - ros2/src/mowgli_interfaces/CMakeLists.txt (locate the srv_files block; identify the 4 lines to remove)
    - ros2/src/mowgli_interfaces/srv/GetNextStrip.srv (read content for awareness — being deleted)
    - ros2/src/mowgli_interfaces/srv/GetCoverageStatus.srv
    - ros2/src/mowgli_interfaces/srv/GetOutlinePath.srv
    - ros2/src/mowgli_interfaces/srv/PreviewPlan.srv
    - ros2/src/mowgli_map/src/map_server_node.cpp (find every function listed in PATTERNS.md §Group 7 — function bodies to delete; also find the constructor's create_service<...> calls for the 4 removed services)
    - ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp (find the strip-planner state members on lines ~491-494 per PATTERNS.md; find any `Strip*` types/aliases)
    - .planning/phases/01-coverage-planner-rewrite/01-PATTERNS.md §"Group 7: map_server_node Cleanup" (the exact function-name list)
  </read_first>
  <files>
    ros2/src/mowgli_interfaces/srv/GetNextStrip.srv (DELETE)
    ros2/src/mowgli_interfaces/srv/GetCoverageStatus.srv (DELETE)
    ros2/src/mowgli_interfaces/srv/PreviewPlan.srv (DELETE)
    ros2/src/mowgli_interfaces/srv/GetOutlinePath.srv (DELETE)
    ros2/src/mowgli_interfaces/CMakeLists.txt (MODIFY — remove the 4 entries from srv_files)
    ros2/src/mowgli_map/src/map_server_node.cpp (MODIFY — delete strip-planner functions, replace local geometry helpers with mowgli_geometry calls)
    ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp (MODIFY — delete strip-planner state members + handler decls + StripLayout type if local)
  </files>
  <action>
**Step 1 — delete .srv files**:
```
rm ros2/src/mowgli_interfaces/srv/GetNextStrip.srv
rm ros2/src/mowgli_interfaces/srv/GetCoverageStatus.srv
rm ros2/src/mowgli_interfaces/srv/PreviewPlan.srv
rm ros2/src/mowgli_interfaces/srv/GetOutlinePath.srv
```

**Step 2 — mowgli_interfaces/CMakeLists.txt**: remove these 4 entries from the `set(srv_files ...)` block:
```
"srv/GetNextStrip.srv"
"srv/GetCoverageStatus.srv"
"srv/PreviewPlan.srv"
"srv/GetOutlinePath.srv"
```
The remaining srv list keeps GetMowingArea, GetAllAreas, MowerControl, etc. unchanged.

**Step 3 — map_server_node.{hpp,cpp} — delete strip-planner code**:

In **map_server_node.hpp**:
- Delete the `StripLayout` struct/type if it lives here (PATTERNS.md says it does); if it lives in a separate header, delete that header too (search via `grep -r "struct StripLayout\|class StripLayout" ros2/src/mowgli_map/`).
- Delete state members:
  - `std::vector<StripLayout> strip_layouts_;`
  - `std::vector<int> current_strip_idx_;`
  - any other strip_* state (use grep on the full hpp file to find them all).
- Delete handler decls for:
  - `on_get_next_strip`
  - `on_get_coverage_status`
  - `on_preview_plan`
  - `on_get_outline_path`
- Delete decls for the geometry helpers (now in mowgli_geometry):
  - `compute_optimal_mow_angle`
  - `convex_hull`
  - `point_in_polygon`
  - `offset_polygon_inward`
- Delete decls for the strip-planner internals:
  - `ensure_strip_layout`
  - `find_next_unmowed_strip`
  - `strip_to_path`
  - `is_strip_mowed` / `is_strip_blocked` (per CONTEXT.md "Reference implementations")
  - `compute_outline_path` (the wrapper)
- Delete the corresponding `#include <mowgli_interfaces/srv/get_next_strip.hpp>`, `get_coverage_status.hpp`, `preview_plan.hpp`, `get_outline_path.hpp` lines.

In **map_server_node.cpp**:
- Delete each of the function bodies listed above.
- Delete the corresponding `create_service<...>` calls in the constructor that registered the 4 removed services.
- Replace any remaining call sites of the deleted geometry helpers with the namespaced `mowgli_geometry::` versions (Plan 02's promotion). For example, if any non-strip-related code still calls `compute_optimal_mow_angle(poly)`, change to `mowgli_geometry::compute_optimal_mow_angle(poly)` and add `#include <mowgli_geometry/geometry.hpp>` at the top.
- Remove dead `#include` lines that pulled the now-deleted .srv generated headers.

**Common pitfall**: the `mow_progress` grid_map layer maintenance code may interweave with the strip planner. CONTEXT.md says progress was tracked there; `mow_progress` itself remains useful for visualization (CLAUDE.md invariant #14 explicitly defines the grid_map → OccupancyGrid convention used by `mow_progress_to_occupancy_grid`). Do NOT delete `mow_progress_to_occupancy_grid` or its publisher — only the strip-planner-specific consumers/producers. If a function is BOTH a strip-planner internal AND a `mow_progress` updater, surgically remove the strip-planner-specific call paths but keep the `mow_progress` writes.

**After deletion, verify**:
- The remaining map_server_node functionality (areas DB CRUD, /get_mowing_area, /add_mowing_area, /clear_map, /clear_obstacle, /set_docking_point, mow_progress publication, recording trajectory pipeline) is fully intact.
- Any unit test that was specifically testing strip-planner internals is also deleted (`grep -r "ensure_strip_layout\|find_next_unmowed_strip" ros2/src/mowgli_map/test/`); replace with a no-op or remove the test file entirely. Do NOT delete tests for the surviving areas DB / dock / mow_progress logic.
  </action>
  <verify>
    <automated>cd ros2 && colcon build --packages-select mowgli_interfaces mowgli_map --event-handlers console_cohesion+ 2>&1 | tail -30 && colcon test --packages-select mowgli_map --event-handlers console_cohesion+ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 .srv files are gone: `! ls ros2/src/mowgli_interfaces/srv/GetNextStrip.srv ros2/src/mowgli_interfaces/srv/GetCoverageStatus.srv ros2/src/mowgli_interfaces/srv/PreviewPlan.srv ros2/src/mowgli_interfaces/srv/GetOutlinePath.srv 2>/dev/null` returns non-zero (none exist).
    - mowgli_interfaces/CMakeLists.txt does NOT reference them: `! grep -q "srv/GetNextStrip.srv\|srv/GetCoverageStatus.srv\|srv/PreviewPlan.srv\|srv/GetOutlinePath.srv" ros2/src/mowgli_interfaces/CMakeLists.txt` succeeds.
    - All 8 PATTERNS-listed function names are GONE from map_server_node.cpp: `! grep -q "ensure_strip_layout\|find_next_unmowed_strip\|strip_to_path\|on_get_next_strip\|on_get_coverage_status\|on_preview_plan\|on_get_outline_path\|compute_outline_path" ros2/src/mowgli_map/src/map_server_node.cpp` succeeds.
    - Local geometry helpers GONE from map_server_node: `! grep -q "^.*offset_polygon_inward\|^.*convex_hull\|^.*compute_optimal_mow_angle\|^.*point_in_polygon" ros2/src/mowgli_map/src/map_server_node.cpp` returns no LOCAL DEFINITIONS (call sites prefixed with `mowgli_geometry::` are allowed; the test passes if no function definition with these names exists in the file. Use `grep -E "^\s*(static\s+|inline\s+|template[^\n]*)*\s*(std::vector|bool|double).*(offset_polygon_inward|convex_hull|compute_optimal_mow_angle|point_in_polygon)\s*\(" ros2/src/mowgli_map/src/map_server_node.cpp` and assert no matches).
    - StripLayout type gone: `! grep -q "struct StripLayout\|class StripLayout\|StripLayout " ros2/src/mowgli_map/src/map_server_node.cpp ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp` succeeds (one or two reads needed).
    - Pull-path service includes gone: `! grep -q "get_next_strip.hpp\|get_coverage_status.hpp\|preview_plan.hpp\|get_outline_path.hpp" ros2/src/mowgli_map/src/map_server_node.cpp ros2/src/mowgli_map/include/mowgli_map/map_server_node.hpp` succeeds.
    - `colcon build --packages-select mowgli_interfaces mowgli_map` exits 0.
    - `colcon test --packages-select mowgli_map` exits 0.
    - map_server_node.cpp shrunk meaningfully: `wc -l ros2/src/mowgli_map/src/map_server_node.cpp` returns at least 800 lines fewer than the pre-cleanup ~3713-line baseline (the deleted functions account for several hundred lines combined).
  </acceptance_criteria>
  <done>4 .srv files gone, 8 functions deleted, all geometry now flows through mowgli_geometry, build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk (areas.yaml) ↔ map_server_node | Operator-edited file; narrow_area_strategy field accepted from disk |
| BT / GUI / planner ↔ map_server_node services | Cross-process callers may supply malformed requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | areas.yaml narrow_area_strategy out of 0..2 range | mitigate | Reader range-checks and clamps to 0 (Skip is the safe default) with RCLCPP_WARN. No path traversal possible (it is a uint8 field, not a path). |
| T-06-02 | Spoofing | A non-planner caller invokes /map_server_node/get_all_areas | accept | Internal LAN-only DDS; no auth layer in this stack (consistent with rest of the project). Returning the area DB to any DDS peer is acceptable. |
| T-06-03 | Information disclosure | GetAllAreas response leaking internal MowingArea bookkeeping | mitigate | The handler explicitly copies only the public fields (name, area, obstacles, is_navigation_area, narrow_area_strategy) into the MapArea msg; internal mow_progress / dock state is NOT included. |
| T-06-04 | Repudiation | Code deletion accidentally removes mow_progress safeguards | mitigate | Acceptance criteria gate: existing unit tests must pass. CLAUDE.md invariant #14 (grid_map → OccupancyGrid convention) is explicitly preserved by the action's "do NOT delete mow_progress_to_occupancy_grid" instruction. |

Severity: low. Cleanup only; no new external attack surface (the new GetAllAreas service is internal IPC per D-12).
</threat_model>

<verification>
- `colcon build --packages-select mowgli_interfaces mowgli_map` exits 0
- `colcon test --packages-select mowgli_map` exits 0 (existing tests pass)
- map_server_node hosts /map_server_node/get_all_areas
- 4 pull-path .srv files are removed; CMakeLists.txt no longer references them
- 8+ pull-path functions are removed from map_server_node.cpp
- Geometry helpers consolidated under mowgli_geometry
- areas.yaml round-trips narrow_area_strategy
</verification>

<success_criteria>
- SPEC R-1 satisfied (continued from Plan 05): GetAllAreas service is live, planner can fetch areas
- SPEC §"Out of scope: Removal-hardening" honored: pull-path is deleted, not deprecated
- D-02 honored: single source of truth for geometry — map_server uses mowgli_geometry, not local copies
- D-12 honored: GetAllAreas service handler exists in map_server_node
- D-08 honored elsewhere; this plan does NOT touch nav2_params.yaml
</success_criteria>

<output>
After completion, create `.planning/phases/01-coverage-planner-rewrite/01-06-SUMMARY.md` describing: which functions were deleted (with line ranges from the original 3713-line file), the new line count of map_server_node.cpp, the GetAllAreas handler implementation, the 4 deleted .srv files, the areas.yaml schema bump for narrow_area_strategy, and the surviving mow_progress logic.
</output>
