---
phase: 01-coverage-planner-rewrite
plan: 09
type: execute
wave: 5
depends_on: [04, 06, 07, 08]
files_modified:
  - ros2/e2e_test.py
  - ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py
  - .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md
autonomous: false
requirements: [R-1, R-2, R-3, R-12]
must_haves:
  truths:
    - "coverage_planner_node is launched as part of the standard mowgli_bringup launch file (added next to map_server_node) so the simulation/runtime starts the new planner automatically"
    - "ros2/e2e_test.py is updated to drive the new BT path (PlanCoverageGoal sends action goal, FollowCoveragePlan executes the plan); old strip-planner-specific assertions removed; new assertions reflect the segment_type-aware plan"
    - "make e2e-test passes against the headless simulation with the new planner active and pull-path artifacts deleted (SPEC AC-12)"
    - "VALIDATION.md per-task verification table is populated with all task IDs from Plans 01-08, mapped to SPEC R-NN, automated commands, and statuses; nyquist_compliant: true is set in the frontmatter"
    - "Hardware checkpoint (manual): operator deploys to Pi5, robot mows >=1 complete strip in Eichenau garden without #61-class (drive-through obstacle outline) or #64-class (10cm BackUp abort) failures (SPEC AC-13). The mow_session_monitor JSONL captures the session for post-mortem."
  artifacts:
    - path: "ros2/e2e_test.py"
      provides: "End-to-end simulation test exercising the new planner + BT"
      contains: "plan_coverage"
    - path: "ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py"
      provides: "Standard launch with coverage_planner_node included"
      contains: "coverage_planner_node"
    - path: ".planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md"
      provides: "Populated per-task verification table"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py"
      to: "mowgli_coverage_planner package executable"
      via: "Node action with package='mowgli_coverage_planner', executable='coverage_planner_node'"
      pattern: "coverage_planner_node"
---

<objective>
Wire the new planner into the standard launch file so the runtime starts it automatically; update the E2E sim test to exercise the new BT path; populate VALIDATION.md per-task table; and gate on a manual hardware smoke test on the Pi5 in the Eichenau garden (SPEC AC-13).

Purpose: This is the integration + acceptance plan. Plans 01-08 delivered the parts; this plan proves they compose end-to-end. The hardware checkpoint is the only path to satisfy SPEC AC-13 (Pi5 garden smoke test) — automation cannot replace it.
Output: launch file modified; e2e_test.py modified; VALIDATION.md populated; checkpoint task pauses for operator confirmation after the Pi5 smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-coverage-planner-rewrite/01-SPEC.md
@.planning/phases/01-coverage-planner-rewrite/01-CONTEXT.md
@.planning/phases/01-coverage-planner-rewrite/01-RESEARCH.md
@.planning/phases/01-coverage-planner-rewrite/01-PATTERNS.md
@.planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md
@CLAUDE.md
@.claude/rules/ros2.md

<interfaces>
Existing E2E infrastructure:
- ros2/e2e_test.py — main E2E entry point (read first to understand current test driver / scenarios)
- ros2/Makefile — `make e2e-test` target (read to confirm invocation; SPEC AC-12 requires this passes)
- ros2/src/mowgli_simulation/ — Gazebo or pure-rclcpp sim setup

Existing launch infrastructure:
- ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py — top-level launch file
- ros2/src/mowgli_bringup/launch/ — additional sub-launch files (search via ls)

Hardware bench access (memory: reference_pi5_testbench.md): SSH to pi@10.10.40.68 for on-hardware verification. Mowgli image deploy chain (memory: feedback_background_pipeline_deploy.md): after `git push`, GH Actions builds Docker images, then auto-deploy to Pi5 on green.

Mow session monitor (CLAUDE.md §"Mowing Session Monitoring"): MUST be run in parallel with the hardware smoke test. Records 10 Hz JSONL with cross-checks (RTK cov verdict, fusion↔gps, wheel↔gyro, fusion↔kinematic-icp). Logs go to docker/logs/mow_sessions/<session_name>.jsonl.

Tags / SPEC failures explicitly to monitor for during hardware smoke (CLAUDE.md + memory):
- #61-class: planner routing footprint through obstacle outlines (Issue #61, recently closed by skip-obstacle-outline-loops fix; new planner must NOT regress)
- #64-class: 10cm BackUp abort (Issue #64, BackUp behavior aborting due to local costmap LETHAL); the new planner doesn't change BackUp logic but the plan's UNDOCK + DOCK_APPROACH segments must navigate cleanly
- LETHAL-cell BackUp aborts during undock
- Drive-through virtual obstacle outlines

VALIDATION.md skeleton table is at line 41-43 (header + one placeholder row). This plan replaces that single row with one row per task (Plans 01-08), formatted per the existing table header.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add coverage_planner_node to standard launch + update E2E sim test for the new BT path</name>
  <read_first>
    - ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py (full file - find map_server_node Node action; mirror its shape for coverage_planner_node)
    - ros2/e2e_test.py (full file - understand test driver: how it triggers COMMAND_START, how it asserts plan progress, how it kills the sim)
    - ros2/Makefile (find e2e-test target; understand setup/teardown)
    - ros2/src/mowgli_bringup/config/mowgli_robot.yaml (after Plan 03 edits - confirm robot_geometry: section exists for the launched coverage_planner_node to load)
    - .planning/phases/01-coverage-planner-rewrite/01-SPEC.md AC-12 (E2E sim test passes with new planner active)
  </read_first>
  <files>
    ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py (MODIFY - add coverage_planner_node Node action)
    ros2/e2e_test.py (MODIFY - update assertions to match new BT path)
  </files>
  <action>
**Step 1 - launch file**: in `ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py`, locate the existing Node action that starts `map_server_node`. Add a sibling Node action immediately after it:
```python
Node(
    package='mowgli_coverage_planner',
    executable='coverage_planner_node',
    name='coverage_planner_node',
    output='screen',
    parameters=[mowgli_robot_config_path],   # the same YAML used by other Mowgli nodes
    # No remappings: relative names resolve to /coverage_planner_node/plan_coverage etc.
)
```

If the launch file uses LaunchConfiguration / DeclareLaunchArgument for tunable params, follow the existing pattern. Confirm `mowgli_robot_config_path` (or the equivalent variable name in the existing file) is the same path that map_server_node reads — coverage_planner_node MUST share the same robot_geometry: source per D-07.

If a separate sub-launch file exists for "the planning stack" (search for `compose_planning.launch.py` / similar), add the new Node there instead. Use the same convention.

**Step 2 - E2E sim test (`ros2/e2e_test.py`)**:

The existing test exercised the legacy strip-planner pull pattern. Update it to exercise the new path:

1. Locate the test setup that starts the simulation + bringup. Confirm the new launch addition (Step 1) means coverage_planner_node is up at the same time as map_server_node.

2. Replace any assertions that checked for legacy services (`/get_next_strip`, `/get_outline_path`, `/get_coverage_status`, `/preview_plan`) with assertions for:
   - `/coverage_planner_node/plan_coverage` action endpoint is live
   - `/map_server_node/get_all_areas` service is live

3. Replace any assertions on legacy BT nodes (e.g., that `/follow_path` was called by `FollowStrip`) with assertions for the new path:
   - On COMMAND_START, the BT's PlanCoverageGoal sends an action goal to `/coverage_planner_node/plan_coverage`
   - The action result has success=true with at least 2 waypoints (UNDOCK + DOCKING minimum)
   - FollowCoveragePlan dispatches at least one FollowPath goal (for the first OUTLINE_WORKING_AREA segment)
   - The blade gets enabled exactly once for the first OUTLINE_WORKING_AREA segment (validated via subscribing to the MowerControl service-call topic, or by spying on the firmware-bridge MowerControl logs)

4. Add a new assertion specifically for SPEC AC-12 + AC-3:
   - For the standard simulation test area (whatever the existing e2e fixture uses), assert the returned plan size is in [50, 200].

5. Update the test's expected timing: planning takes 5-30s per CONTEXT.md D-04. If the test uses a fixed `time.sleep(...)` shorter than 30s before checking plan progression, raise it accordingly.

6. Remove any test cases that explicitly tested strip-planner fallback (`test_strip_skipped_recovery`, `test_strip_retry_on_blocked`, etc.) — those code paths no longer exist after Plan 06.

7. If the E2E test framework uses Python `unittest` or `launch_testing`, follow whichever pattern the existing file uses. Do not introduce a new framework.

**Step 3 - update install/exec_depend in mowgli_bringup/package.xml**: add `<exec_depend>mowgli_coverage_planner</exec_depend>` so the bringup package's runtime knows to install the new package's binaries. Verify with `grep -q "mowgli_coverage_planner" ros2/src/mowgli_bringup/package.xml`.
  </action>
  <verify>
    <automated>cd ros2 && colcon build --packages-select mowgli_bringup --event-handlers console_cohesion+ 2>&1 | tail -15 && cd ros2 && timeout 600 make e2e-test 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "package='mowgli_coverage_planner'\|package=\"mowgli_coverage_planner\"" ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py` succeeds (or whichever sub-launch file the new Node was added to).
    - `grep -q "executable='coverage_planner_node'\|executable=\"coverage_planner_node\"" ros2/src/mowgli_bringup/launch/mowgli_bringup.launch.py` succeeds.
    - `grep -q "mowgli_coverage_planner" ros2/src/mowgli_bringup/package.xml` succeeds.
    - `colcon build --packages-select mowgli_bringup` exits 0.
    - `! grep -q "/get_next_strip\|/get_outline_path\|/get_coverage_status\|/preview_plan\|FollowStrip\|GetNextStrip\|TransitToStrip\|OutlineArea\|GetNextUnmowedArea" ros2/e2e_test.py` succeeds (legacy test assertions are gone).
    - `grep -q "plan_coverage\|PlanCoverageGoal\|FollowCoveragePlan" ros2/e2e_test.py` succeeds (new assertions present).
    - `make e2e-test` exits 0 within 10 minutes (SPEC AC-12).
    - The plan-size assertion (50<=N<=200) is present in e2e_test.py: `grep -E "50.*<=.*plan|len\(plan\).*>=.*50" ros2/e2e_test.py` returns matches.
  </acceptance_criteria>
  <done>Launch file includes the new node; E2E sim test passes with the new BT path; legacy assertions are gone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate VALIDATION.md per-task verification table + set nyquist_compliant: true</name>
  <read_first>
    - .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md (full file - existing skeleton + Wave 0 list)
    - All 8 PLAN files (01-01 through 01-08-PLAN.md) - extract task IDs, requirement IDs, automated commands
    - .planning/phases/01-coverage-planner-rewrite/01-SPEC.md §"Acceptance Criteria" (16 ACs)
  </read_first>
  <files>
    .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md (MODIFY - replace placeholder row in the table; flip nyquist_compliant frontmatter to true)
  </files>
  <action>
Open `.planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md`.

**Frontmatter change**:
```diff
- nyquist_compliant: false
+ nyquist_compliant: true
- wave_0_complete: false
+ wave_0_complete: true
```

**Per-task table population**: replace the single placeholder row (line 43) with one row for EACH task in Plans 01 through 08 (16 tasks total — 2 tasks per plan). Use the table header from line 41 verbatim. Each row maps a task to its primary SPEC requirement and the automated command from its `<verify>` block.

Example rows (planner generates the full table by reading each PLAN file's task `<name>` + `<verify>` blocks):

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-01-T1 | 01 | 1 | R-2, R-4, R-5, R-12, R-13 | T-01-* | Schema-only, no runtime input | unit (rosidl gen) | `colcon build --packages-select mowgli_interfaces` | yes | ⬜ pending |
| 01-01-T2 | 01 | 1 | R-2, R-12 | T-01-01..04 | Field validation in planner (Plan 05) | unit (rosidl gen) | `colcon build --packages-select mowgli_interfaces` | yes | ⬜ pending |
| 01-01-T3 | 01 | 1 | (none — codegen only) | — | No runtime exposure | typecheck | `cd gui && go build ./... && cd web && yarn tsc --noEmit` | yes | ⬜ pending |
| 01-02-T1 | 02 | 2 | R-6, R-7 | T-02-03 | Caller-validated polygons | unit | `colcon test --packages-select mowgli_geometry` | yes | ⬜ pending |
| 01-02-T2 | 02 | 2 | R-6, R-10, R-13 | T-02-01..02 | Atomic FS write; PCA bounded | unit | `colcon test --packages-select mowgli_geometry` | yes | ⬜ pending |
| 01-03-T1 | 03 | 2 | R-6 | (none) | Config defaults | yaml-parse | `python3 -c "import yaml; yaml.safe_load(...)"` | yes | ⬜ pending |
| 01-03-T2 | 03 | 2 | R-6 | (none) | Documentation | grep | `grep -q "Architecture Invariant #15" CLAUDE.md` | yes | ⬜ pending |
| 01-04-T1 | 04 | 2 | R-2, R-4 | T-04-01,03,04 | XSS-safe via JSX, AsyncButton DoS guard | typecheck+lint | `cd gui/web && yarn tsc --noEmit && yarn lint` | yes | ⬜ pending |
| 01-04-T2 | 04 | 2 | R-13 | T-04-05 | Range-clamped narrow-area dropdown | typecheck+lint+unit | `cd gui/web && yarn test --run && yarn tsc --noEmit && yarn lint` | yes | ⬜ pending |
| 01-05-T1 | 05 | 3 | R-1, R-2, R-12 | T-05-05,06 | Concurrent-goal reject; param validation | build | `colcon build --packages-select mowgli_coverage_planner` | yes | ⬜ pending |
| 01-05-T2 | 05 | 3 | R-10, R-11, R-12 | T-05-01..04 | Atomic write; corruption detection | unit | `colcon test --packages-select mowgli_coverage_planner` | yes | ⬜ pending |
| 01-06-T1 | 06 | 3 | R-1, R-13 | T-06-01..03 | Range clamp on disk read; internal-only service | unit | `colcon test --packages-select mowgli_map` | yes | ⬜ pending |
| 01-06-T2 | 06 | 3 | R-1 | T-06-04 | Cleanup gated on existing tests | unit | `colcon test --packages-select mowgli_interfaces mowgli_map` | yes | ⬜ pending |
| 01-07-T1 | 07 | 4 | R-3, R-4, R-5, R-6, R-7, R-8, R-12, R-13 | T-07-01,02,07,08 | Pre+post validators reject unsafe plans | unit | `colcon test --packages-select mowgli_coverage_planner` | yes | ⬜ pending |
| 01-07-T2 | 07 | 4 | R-9, R-11 | T-07-05 | Resume tolerance gate; finite-value guard | unit | `colcon test --packages-select mowgli_coverage_planner` | yes | ⬜ pending |
| 01-08-T1 | 08 | 4 | R-2, R-4, R-10 | T-08-01,02,05,06 | Blade rules per segment_type; safety | build+unit | `colcon test --packages-select mowgli_behavior` | yes | ⬜ pending |
| 01-08-T2 | 08 | 4 | R-4 | T-08-03 (HIGH) | onHalted disables blade; regression-tested | unit | `colcon test --packages-select mowgli_behavior` | yes | ⬜ pending |
| 01-09-T1 | 09 | 5 | R-1, R-2, R-3, R-12 | (composite) | E2E acceptance | sim | `cd ros2 && make e2e-test` | yes | ⬜ pending |
| 01-09-T2 | 09 | 5 | (validation) | (composite) | Doc completion | grep | `grep -q "nyquist_compliant: true" .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md` | yes | ⬜ pending |
| 01-09-T3 | 09 | 5 | R-1 (AC-13) | (composite) | Hardware smoke | manual | (operator verification) | n/a | ⬜ pending |

After committing the populated table, the executor running this task confirms each row's automated command (where automated) was run during the corresponding Plan 01-08 task and recorded a green status. If any rows still show ⬜ pending after Plans 01-08 completed, escalate before invoking Task 3 below.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md && grep -c "01-0[1-9]-T[0-9]" .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION.md frontmatter has `nyquist_compliant: true`.
    - VALIDATION.md frontmatter has `wave_0_complete: true`.
    - Per-task table contains at least 19 task rows (16 from Plans 01-08 + 3 from Plan 09): `grep -c "^| 01-0" .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md` returns >= 19.
    - Every row has either an automated command or "manual" with a justification (no row has an empty automated-command cell unless the row is for the hardware checkpoint).
    - SPEC AC-13 explicitly mapped to row 01-09-T3 (the hardware smoke).
  </acceptance_criteria>
  <done>VALIDATION.md per-task table populated; all SPEC requirements mapped to a verifiable task; nyquist_compliant flag flipped to true.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN — Pi5 hardware smoke test in Eichenau garden (SPEC AC-13)</name>
  <what-built>
    A working `coverage_planner_node` action server, a `FollowCoveragePlan` BT node, footprint-aware geometry, atomic checkpoint persistence, GUI Preview Plan rendering, and an E2E sim test that passes. Hardware verification on the Pi5 in the actual Eichenau garden (memory: user_hardware.md — YardForce 500 + Pi5 + UM980 + LD19 LiDAR + local NTRIP base + datum 48.159, 11.315) is required to satisfy SPEC AC-13.
  </what-built>
  <how-to-verify>
**Pre-deploy checks (do these before driving to the bench):**
1. Confirm GH Actions Docker build for the latest commit is green. Per memory feedback_background_pipeline_deploy.md, the auto-deploy to Pi5 fires on green.
2. Confirm the Pi5 has the new image: `ssh pi@10.10.40.68 "docker ps --format '{{.Image}}' | sort -u"` should show the latest tag built from this branch.

**On-bench setup:**
3. Place the YardForce 500 on its dock in the Eichenau garden. Confirm RTK base is online and serving NTRIP corrections.
4. Power up. Wait for `/gps/fix` status to reach RTK-Fixed (covariance σ ~3 mm).
5. Open the GUI on a laptop. Confirm at least one working area is defined and reachable. If areas were defined under the legacy schema (no narrow_area_strategy field), confirm areas.yaml round-trip on disk preserves them with the default strategy=0 (Skip) — open `EditAreaModal` for each area, confirm the dropdown defaults to "Skip" and saves correctly.

**Start the session monitor (mandatory per CLAUDE.md §"Mowing Session Monitoring"):**
6. From the laptop / via SSH:
```bash
ssh pi@10.10.40.68
docker exec -d mowgli-ros2 bash -c 'source /opt/ros/kilted/setup.bash && source /ros2_ws/install/setup.bash && python3 /ros2_ws/scripts/mow_session_monitor.py --session 2026-04-28-coverage-rewrite-smoke-v1 --output-dir /ros2_ws/maps'
```

**Smoke run:**
7. From the GUI, click **Preview Plan** first (does NOT engage motors). Confirm:
   - The plan renders with the 8-color segment_type palette (D-11 colors visible: green/dark-green/blue/orange/grey/amber/dark-amber/yellow).
   - Plan size is reasonable (typically 50-500 waypoints for the bench area).
   - No `notification.error` toast appeared.
8. Click **Start** (sends COMMAND_START via HighLevelControl.srv).
9. Watch the robot:
   - It UNDOCKs (orange UNDOCK segment color) using BackUp behavior at 0.15 m/s, traveling exactly `undock_distance` rearward.
   - It TRANSITs to the first working area (grey TRANSIT color).
   - It runs OUTLINE_WORKING_AREA passes (green) — blade ON.
   - It enters MOWING_BOUSTROPHEDON (blue) — alternating swath direction visible.
   - It does NOT cross any obstacle outlines (regression: #61 — closed but new planner must not regress).
   - BackUp recovery does NOT abort within 10 cm of dock-leave (regression: #64 — closed but new planner must not regress).
10. Let the robot complete AT LEAST ONE full strip (one MOWING_BOUSTROPHEDON pair from start to end). 11. Use the GUI Stop button to send COMMAND_HOME, OR (if the robot finishes) wait for the auto-return-to-dock sequence:
   - RETURN_TO_DOCK (yellow) -> DOCK_APPROACH (amber) -> DOCKING (dark amber)
   - Robot lands on the dock with charging current visible.

**Post-run verification:**
12. Confirm a checkpoint file exists at `<areas_dir>/coverage_<area_index>.kv` per Plan 05's persistence:
```bash
ssh pi@10.10.40.68 "docker exec mowgli-ros2 cat /ros2_ws/maps/coverage_0.kv"
```
Expected: 9 key=value lines (current_outline_index, current_swath_index, swath_direction, last_completed_swath_index, next_open_swath_index, last_mow_angle_deg, last_swath_endpoint_x/y/yaw).

13. Resume test:
   - With the robot back on the dock, click Start AGAIN.
   - Confirm the new plan starts the resume swath very close to where the previous run ended (the GUI may show the resume MOWING_BOUSTROPHEDON pose marker near the persisted endpoint).
   - SPEC R-11 tolerance is <=5 cm + 5° yaw — eyeball verification at the bench is sufficient. Tighter verification comes from the unit test in Plan 07.

14. Stop the session monitor (Ctrl-C inside the docker exec, or kill the bg process). The summary record (last line of the JSONL) should report:
    - rtk_cov_check.verdict: "healthy" (RTK was good throughout)
    - peak_fusion_gps_error: small (<10 cm)
    - peak_wheel_gyro_yaw_drift: small
    - final_bt_state: HIGH_LEVEL_STATE_IDLE (charging)

15. Copy the JSONL to the repo for golden-run preservation:
```bash
scp pi@10.10.40.68:/path/to/docker/logs/mow_sessions/2026-04-28-coverage-rewrite-smoke-v1.jsonl docker/logs/mow_sessions/
git add docker/logs/mow_sessions/2026-04-28-coverage-rewrite-smoke-v1.jsonl
```

**Acceptance criteria for this checkpoint** (operator confirms each):
- [ ] Plan generated successfully (Preview Plan + Start both surfaced a non-empty plan)
- [ ] All 10 SPEC validation points passed (no error toast surfaced; coverage_planner_node logs show no PlanError)
- [ ] At least one complete MOWING_BOUSTROPHEDON strip executed end-to-end
- [ ] No #61-class drive-through obstacle outline failure
- [ ] No #64-class 10cm BackUp abort failure
- [ ] Checkpoint .kv file written and parses
- [ ] Resume run starts within ≈5 cm + 5° of persisted endpoint (visual verification at bench)
- [ ] Session monitor JSONL committed to docker/logs/mow_sessions/
- [ ] No emergency stop fired during the run (firmware safety latch did not trigger)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 9 acceptance criteria above are met. Otherwise describe the failure mode in detail (which segment_type was active, which waypoint sequence_id, what the firmware emergency latch reported if any, what the session monitor cross_checks showed). On any failure, the appropriate fix path is:
    - Plan/path geometry issue -> revise Plan 07 (validators or PlanBuilder)
    - BT dispatch / blade-timing issue -> revise Plan 08
    - Action plumbing issue -> revise Plan 05
    - Config / launch issue -> revise this plan (Task 1)
    - Hardware-only issue (RTK lost, dock alignment off) -> not a code regression; reschedule
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator ↔ robot (Start button) | The hardware smoke test sends the robot motor commands in the real world |
| Pi5 ↔ Mowgli mainline (auto-deploy) | The auto-deploy chain ships Docker images to the bench; a forged tag could ship a bad image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Auto-deployed Docker image with bad code reaches the Pi5 | mitigate | Pre-deploy check #1 + #2 (operator confirms green CI + image tag matches the branch HEAD). Manual deploy can be skipped if any GH Actions run is red. |
| T-09-02 | Trust integrity | Robot drives off the work area on first deploy | mitigate (HIGH severity — physical safety) | Pre-flight validators in Plan 07 reject any plan that violates the 10 SPEC validation points BEFORE the BT runs FollowCoveragePlan. The Preview Plan step (acceptance criterion 7 in checkpoint instructions) is a hard gate before clicking Start. Firmware emergency latch + collision_monitor are the runtime safety nets. |
| T-09-03 | DoS | Session monitor saturating SD card | accept | 10 Hz JSONL is < 1 MB/min; bench-test sessions are < 30 min; SD-class quality is operator's responsibility. |
| T-09-04 | Information disclosure | mow_session_monitor JSONL committed to git contains GPS coordinates + dock pose | accept | The Eichenau garden is the operator's own property; coordinates are already in the public memory note (datum 48.159, 11.315). No new exposure. |
| T-09-05 | Repudiation | Smoke-test outcome contested | mitigate | The session monitor JSONL with metadata header (git branch + commit + dirty + image tags + config hashes) is a tamper-evident record of what code ran in the test. Committed to git. |

Severity: HIGH on T-09-02. The mitigation chain (planner pre-validation + Preview Plan visual check + firmware safety latch + collision_monitor) is multi-layered. The checkpoint operator has explicit veto authority — they only proceed past Step 8 ("Click Start") after the Preview Plan visual confirms a sane plan.
</threat_model>

<verification>
- `make e2e-test` exits 0 within 10 minutes
- coverage_planner_node is part of the standard launch (verifiable via `ros2 node list` after `ros2 launch mowgli_bringup mowgli_bringup.launch.py`)
- VALIDATION.md per-task table populated for all 19+ tasks; `nyquist_compliant: true` set
- Hardware checkpoint passes (operator confirms 9 acceptance criteria)
- Session monitor JSONL committed to docker/logs/mow_sessions/ for the smoke-test session
</verification>

<success_criteria>
- SPEC AC-12 satisfied: `make e2e-test` passes with the new planner active and pull-path artifacts deleted
- SPEC AC-13 satisfied: Pi5 garden smoke test passes; >=1 complete strip; no #61/#64-class regressions
- SPEC AC-15 partially satisfied: Preview Plan button visually renders the plan with segment_type colour coding (operator visual gate; full UI sign-off may follow in a separate UI-checker pass)
- All 13 SPEC requirements (R-1..R-13) have at least one passing test or hardware-verified gate
- VALIDATION.md is complete and `nyquist_compliant: true`
</success_criteria>

<output>
After completion, create `.planning/phases/01-coverage-planner-rewrite/01-09-SUMMARY.md` describing: launch-file change, e2e_test.py changes, the populated VALIDATION.md table summary, the smoke-test session ID + JSONL path + verdict, and a final SPEC-AC checklist showing which acceptance criteria are green / yellow / blocked.
</output>
