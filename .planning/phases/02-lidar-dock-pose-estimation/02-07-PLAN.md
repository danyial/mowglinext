---
phase: 02-lidar-dock-pose-estimation
plan: 07
type: execute
wave: 4
depends_on: [01, 06]
files_modified:
  - ros2/src/mowgli_behavior/trees/main_tree.xml
  - gui/pkg/providers/ros.go
  - gui/pkg/api/mowglinext.go
  - gui/web/src/hooks/useDockMatch.ts
  - gui/web/src/components/MowerStatus.tsx
  - gui/web/src/components/DockMatchCard.tsx
autonomous: true
requirements: [R-9, R-10]
must_haves:
  truths:
    - "grep -c '<DockRobot ' ros2/src/mowgli_behavior/trees/main_tree.xml returns 0 (R-10 — counts only literal self-closing tags; preserves the 8 historical XML comments at lines 84/123/463/481/503/515/518/520 that document the deprecated API)"
    - "All 6 former DockRobot sites now contain <Sequence><ApproachDock/><FineDock/></Sequence> (or equivalent named subtree)"
    - "UndockSequence extended: PreUndockClearanceCheck before BackUp; PostUndockRtkValidation after CalibrateHeadingFromUndock; RecordDockApproachPose at the end"
    - "OuterReactiveSequence guards FineDock so E-Stop reset does NOT auto-restart the dock cycle (R-9)"
    - "topicMap in gui/pkg/providers/ros.go has dockMatchPose + dockMatchConfidence keys (D-12 reconciliation: Go-relay topic-map pattern, NOT direct foxglove WebSocket)"
    - "useDockMatch hook subscribes via /api/mowglinext/subscribe/dockMatchConfidence + /api/mowglinext/subscribe/dockMatchPose"
    - "DockMatchCard renders status-badge (green/yellow/red per D-10 thresholds) + Inlier_% / RMSE_cm pair + dock_scan age in days + last fine-dock lateral error in cm"
    - "Recapture button in DockMatchCard opens confirm-modal (D-11) before invoking calibrate_imu_yaw_node"
    - "BT XML still passes BehaviorTreeFactory::createTreeFromText validation (no syntax error from the migration)"
  artifacts:
    - path: "ros2/src/mowgli_behavior/trees/main_tree.xml"
      provides: "Migrated BT — 6 DockRobot sites replaced; UndockSequence extended with 3 new BT nodes"
      contains: "<ApproachDock/>"
    - path: "gui/web/src/hooks/useDockMatch.ts"
      provides: "React hook subscribing to /dock_match/{pose, confidence} via topicMap (mirrors useMagYaw pattern)"
      contains: "useDockMatch"
    - path: "gui/web/src/components/DockMatchCard.tsx"
      provides: "Dock-card extension with confidence badge + numeric pair + Recapture modal"
      contains: "DockMatchCard"
  key_links:
    - from: "ros2/src/mowgli_behavior/trees/main_tree.xml"
      to: "ApproachDock + FineDock BT nodes (Plan 02-06)"
      via: "<Sequence><ApproachDock/><FineDock/></Sequence>"
      pattern: "ApproachDock"
    - from: "gui/pkg/providers/ros.go"
      to: "/dock_match/pose + /dock_match/confidence (Plan 02-04)"
      via: "topicMap entries with logical keys dockMatchPose + dockMatchConfidence"
      pattern: "dockMatch"
    - from: "gui/web/src/components/DockMatchCard.tsx"
      to: "useDockMatch hook"
      via: "import + render"
      pattern: "useDockMatch"
---

<objective>
Wire the Phase 2 BT nodes into operator-visible behavior:

1. **Migrate all 6 `<DockRobot ...>` callsites in main_tree.xml** to the new `<Sequence><ApproachDock/><FineDock/></Sequence>` subtree (or a named LidarDockSubtree if the XML readability benefits — operator preference). Per SPEC R-10 the acceptance is `grep -c "DockRobot" returns 0`. The DockRobot sites confirmed earlier: lines 171, 332, 378, 445, 491, 529 (the SPEC said 4 but greppable count is 6 — 2 additional inside ReactiveFallback patterns must also migrate per the literal R-10 acceptance criterion).
2. **Extend UndockSequence**: insert `PreUndockClearanceCheck` before `BackUp`; insert `PostUndockRtkValidation` after `CalibrateHeadingFromUndock`; insert `RecordDockApproachPose` at the end.
3. **R-9 enforcement**: the OuterReactiveSequence must guard FineDock such that E-Stop reset does NOT auto-restart it. The pattern per CLAUDE.md "Emergency Auto-Reset" is: emergency clears -> robot stays IDLE -> Operator must explicitly re-issue COMMAND_HOME to trigger a fresh ApproachDock+FineDock cycle.
4. **GUI Dock-card extension**: add useDockMatch hook + DockMatchCard component per D-09/D-10/D-11/D-12. D-12 reconciliation locked in CONTEXT.md/RESEARCH.md is to use the existing `topicMap` Go-relay pattern (mirrors useMagYaw, useDockingSensor) rather than `@foxglove/ws-protocol` direct browser-side. Two-line `topicMap` extension + matching dispatch case + new TS hook + new card component.

Per CLAUDE.md "Safety - READ FIRST" and AI #10: this plan is the safety-critical-path migration. Every existing dock-trigger code path is being replaced. Operator-gated 5-of-5 hardware acceptance is Plan 02-08; this plan is the automatable scope.

Purpose: Operator's COMMAND_HOME, low-battery dock, rain-dock, and failed-coverage-dock paths all get the new closed-loop docking behavior consistently. GUI surfaces the LiDAR confidence so operator knows whether the next dock will be reliable.
Output: main_tree.xml migrated; GUI Dock-card extension live; operator can verify confidence in real-time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-lidar-dock-pose-estimation/02-SPEC.md
@.planning/phases/02-lidar-dock-pose-estimation/02-CONTEXT.md
@.planning/phases/02-lidar-dock-pose-estimation/02-RESEARCH.md
@.planning/phases/02-lidar-dock-pose-estimation/02-06-SUMMARY.md
@CLAUDE.md
@.claude/rules/ros2.md

<interfaces>
Plan 02-06 produced (BT nodes registered with the BT factory under canonical names):
- RecordDockApproachPose (SyncActionNode, no input ports required)
- ApproachDock (StatefulActionNode, input port `dock_approach_path`)
- FineDock (StatefulActionNode, input ports `crawl_speed_ms`, `k_lateral`, `k_yaw`, `dock_approach_path`)
- PreUndockClearanceCheck (SyncActionNode, input ports `min_undock_distance_m`, `rear_safety_buffer_m`, `rear_sector_half_deg`)
- PostUndockRtkValidation (SyncActionNode, input ports `warn_threshold_m`, `fail_threshold_m`)

Plan 02-04 produced (GUI subscribes via Go-relay):
- /dock_match/pose (PoseWithCovarianceStamped)
- /dock_match/confidence (mowgli_interfaces::msg::DockMatchConfidence with inlier_ratio + rmse_m + trusted)

Plan 02-01 produced (auto-generated TS bindings):
- gui/web/src/types/ros.ts has `DockMatchConfidence` type with snake_case fields

Existing today (read these before editing):
- ros2/src/mowgli_behavior/trees/main_tree.xml — 6 `<DockRobot dock_id="home_dock" dock_type="simple_charging_dock"/>` sites at the lines reported in SPEC.md. Read full surrounding context for EACH site so the replacement preserves the surrounding Sequence/Fallback structure exactly.
- gui/pkg/providers/ros.go (lines 25-60 — topicMap definition; this plan adds 2 entries)
- gui/pkg/api/mowglinext.go (line 253 — dispatch pattern for new keys; mirror existing case for magYaw/dockingSensor)
- gui/web/src/hooks/useMagYaw.ts (40 lines — verbatim template for useDockMatch.ts)
- gui/web/src/hooks/useDockingSensor.ts (similar template — mirror its idiom for the confidence-bool gate)
- gui/web/src/components/MowerStatus.tsx (one of the existing dock-related cards; identify where to render the new DockMatchCard)
- gui/web/src/components/settings/DockingSection.tsx (existing dock settings card — DockMatchCard is its peer or sub-component depending on UI hierarchy; pick whichever makes operator visibility cleanest)

Topic map entries to add (D-12 reconciliation locked):
```go
"dockMatchPose":       {"/dock_match/pose", "geometry_msgs/msg/PoseWithCovarianceStamped"},
"dockMatchConfidence": {"/dock_match/confidence", "mowgli_interfaces/msg/DockMatchConfidence"},
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate 6 DockRobot sites in main_tree.xml + extend UndockSequence + verify XML still parses</name>
  <read_first>
    - ros2/src/mowgli_behavior/trees/main_tree.xml (FULL — locate every DockRobot site and the surrounding Sequence/Fallback/ReactiveSequence wrapper for each; locate UndockSequence and the existing BackUp + RecordUndockStart + CalibrateHeadingFromUndock subtree; identify the OuterReactiveSequence per RESEARCH §Pattern 4 last bullet — the location where E-Stop reset re-evaluation happens)
    - .planning/phases/02-lidar-dock-pose-estimation/02-SPEC.md R-10 acceptance ("grep -c DockRobot returns 0")
    - .planning/phases/02-lidar-dock-pose-estimation/02-RESEARCH.md §Architecture Diagram "Dock-time sub-pipeline" (the canonical replacement subtree shape)
    - .planning/phases/02-lidar-dock-pose-estimation/02-RESEARCH.md §Architecture Diagram "Undock-time sub-pipeline" (where Pre/Post + RecordDockApproachPose insert)
    - CLAUDE.md "Emergency Auto-Reset" (R-9 semantics: emergency clears -> robot stays IDLE -> operator must re-issue COMMAND_HOME explicitly)
    - .planning/phases/01-coverage-planner-rewrite/01-08-SUMMARY.md (Phase 1 main_tree.xml migration approach — 5 BT nodes deleted + 2 added; this plan mirrors that style for the dock subtree)
  </read_first>
  <files>
    ros2/src/mowgli_behavior/trees/main_tree.xml (MODIFY)
  </files>
  <behavior>
    Each of the 6 `<DockRobot dock_id="home_dock" dock_type="simple_charging_dock"/>` calls is replaced by:
    ```xml
    <Sequence name="LidarDock">
      <ApproachDock dock_approach_path="/ros2_ws/maps/dock_approach.yaml"/>
      <FineDock dock_approach_path="/ros2_ws/maps/dock_approach.yaml"/>
    </Sequence>
    ```
    Preserve every surrounding wrapper (the parent Fallback, Sequence, ReactiveFallback at each site stays exactly as today). The replacement is one BT-node element swapped for one Sequence subtree.

    UndockSequence (the subtree that runs at COMMAND_START and after each charging cycle when re-undocking) gets three insertions:
    1. `<PreUndockClearanceCheck min_undock_distance_m="1.5" rear_safety_buffer_m="0.20" rear_sector_half_deg="30.0"/>` — added BEFORE the existing `<BackUp .../>` (or whatever the BackUp action is named in the current tree). If PreUndockClearanceCheck fails, UndockSequence aborts before motion (Sequence semantics).
    2. `<PostUndockRtkValidation warn_threshold_m="0.5" fail_threshold_m="1.5"/>` — added AFTER the existing `<CalibrateHeadingFromUndock/>`. If FAILURE (>1.5 m discrepancy), UndockSequence aborts; operator alerted via the WARN log line + dock_pose_suspect flag.
    3. `<RecordDockApproachPose/>` — added at the very END of UndockSequence. Always SUCCESS unless an internal error; it persists the post-undock pose for next docking AND triggers R-11 auto-refresh.

    **R-9 OuterReactiveSequence guard — LOCKED VERDICT (BLOCKER 3 fix)**: existing latches are SUFFICIENT — NO new guard needed. Verified deterministically by re-reading main_tree.xml at plan-revision time (2026-04-29):

    Each dock callsite is gated by an `IsCommand` (or `IsBatteryLow` for the critical-battery path) precondition that is NOT auto-restored across an E-Stop cycle:

    | Line | Wrapper | Precondition gating the surrounding Sequence | E-Stop re-entry possible? |
    |------|---------|----------------------------------------------|---------------------------|
    | 171 | `Sequence name="CriticalBatteryDock"` | `IsBatteryLow threshold="10.0"` (line 165) | Only if battery STILL <10% — independent of E-Stop; that path is always-on by design |
    | 332 | `Sequence name="RainDockAndResume"` (inside MowingCommandGuard) | `IsCommand command="1"` at line 318 (MowingSequence root) | NO — operator must re-issue COMMAND_START |
    | 378 | `Sequence name="BatteryDockAndResume"` (inside MowingCommandGuard) | `IsCommand command="1"` at line 318 | NO — same |
    | 445 | `Sequence name="FailedCoverageDock"` (inside MowingCommandGuard) | `IsCommand command="1"` at line 318 | NO — same |
    | 491 | `MowingCompleteAutoDock` (inside MowingSequence) | `IsCommand command="1"` at line 186 + ClearCommand at line 494 | NO — command cleared after dock; operator must re-issue |
    | 529 | `Sequence name="HomeAction"` (inside HomeSequence ReactiveSequence) | `IsCommand command="2"` at line 510 + ClearCommand at line 538 | NO — command cleared after dock |

    **Key invariant**: EmergencyHandler at line 34 does NOT call `ClearCommand`, but it also does NOT re-set the command. After ResetEmergency clears the firmware latch, control returns to MainLogic Fallback. Each dock callsite re-evaluates its `IsCommand`/`IsBatteryLow` precondition. For COMMAND_HOME / COMMAND_START paths, the operator must explicitly re-issue the command — `ClearCommand` is fired at the end of the previous dock cycle (lines 179, 494, 538) precisely so the next entry requires explicit operator intent. For CriticalBatteryDock, re-entry on persistent low battery is the desired safety behavior (independent of E-Stop), so no guard is needed there either.

    **R-9 acceptance** (LOCKED): no new BT XML element introduced for R-9. The acceptance signal is:
    - `grep -c "IsCommand command=\"1\"" ros2/src/mowgli_behavior/trees/main_tree.xml` >= 1 (MowingCommandGuard guard present at line 318)
    - `grep -c "IsCommand command=\"2\"" ros2/src/mowgli_behavior/trees/main_tree.xml` >= 1 (HomeSequence guard present at line 510)
    - `grep -c "ClearCommand" ros2/src/mowgli_behavior/trees/main_tree.xml` >= 6 (cleared after each terminal dock + after each fail/timeout per existing structure)
    - The Plan 02-06 unit test `FineDockHaltsOnEmergency` (case 9) verifies the in-node onHalted publish_zero. This plan adds NO XML guard.

    Document the verdict in 02-07-SUMMARY.md as: "R-9 OuterReactiveSequence: existing IsCommand-gated dock callsites + ClearCommand-after-dock pattern + EmergencyHandler-does-not-restore-command together prevent auto-restart. NO new XML guard added. Verified at plan-revision time against main_tree.xml line numbers 165, 170-180, 318, 510, 538." 
  </behavior>
  <action>
1. Read main_tree.xml in full (or grep -n "DockRobot" to find all 6 line numbers, then sed -n '<line-30>,<line+30>p' for each to capture surrounding context).

2. For each of the 6 DockRobot sites, replace the single self-closing tag with the LidarDock Sequence per <behavior>. Preserve indentation. Preserve any sibling nodes (e.g., the `<AlwaysSuccess/>` fallback inside RainNavOrStay).

3. Locate UndockSequence in the XML. Insert the 3 new node references at the points specified.

4. Verify the XML still parses by running:
```bash
cd ros2 && colcon build --packages-select mowgli_behavior --event-handlers console_cohesion+
ros2 run mowgli_behavior behavior_tree_node --ros-args -p "tree_xml_validation_only:=true" 2>&1 | head -20
```
If `tree_xml_validation_only` parameter does not exist in behavior_tree_node, run the full node briefly: `timeout 5 ros2 run mowgli_behavior behavior_tree_node` and grep stderr for any BT.CPP parse errors. If there are parse errors (typically reported at `[bt_factory] line X column Y`), fix syntax — typical errors are missing `/`, mismatched tag names, or unknown node-type names that don't match the factory registrations from Plan 02-06.

5. Run the existing BT integration test (Phase 1 already has one for main_tree.xml structure) to confirm no regressions: `colcon test --packages-select mowgli_behavior` and verify all existing tests still pass.

6. R-9 OuterReactiveSequence verification (locked at plan-revision time per BLOCKER 3 fix): the verdict is **existing latches sufficient — NO new guard needed**. Confirm at execute time by re-running the 4 acceptance greps (IsCommand command="1", IsCommand command="2", ClearCommand count, FineDockHaltsOnEmergency unit test). Document the verdict in SUMMARY exactly per the locked language in <behavior> above; do NOT introduce a `<WasInDockSubtree/>` node or any other XML guard.

CRITICAL — per CLAUDE.md "Safety - READ FIRST": this is the LARGEST safety-critical change of the phase. Every existing operator-visible dock cycle is being rerouted. The Pi5 5-of-5 hardware acceptance in Plan 02-08 is the ground-truth verification — until that passes, no production deployment is safe. Document this risk explicitly in the SUMMARY.

CRITICAL — per CLAUDE.md "What NOT to Do" + AI #10: this migration completes the deprecation of opennav_docking::DockRobot for production use. The opennav_docking package itself remains installed (per CONTEXT.md "Out of scope: Replacing opennav_docking framework entirely") so future work can re-enable it as a fallback if needed.
  </action>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/mowglinext-danyial/ros2 && colcon build --packages-select mowgli_behavior --event-handlers console_cohesion+ 2>&1 | tail -10 && grep -c "<DockRobot " ros2/src/mowgli_behavior/trees/main_tree.xml && grep -c "ApproachDock" ros2/src/mowgli_behavior/trees/main_tree.xml && grep -c "FineDock" ros2/src/mowgli_behavior/trees/main_tree.xml && grep -c "PreUndockClearanceCheck" ros2/src/mowgli_behavior/trees/main_tree.xml && grep -c "PostUndockRtkValidation" ros2/src/mowgli_behavior/trees/main_tree.xml && grep -c "RecordDockApproachPose" ros2/src/mowgli_behavior/trees/main_tree.xml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<DockRobot " ros2/src/mowgli_behavior/trees/main_tree.xml` returns 0 (R-10 acceptance — BLOCKER 1 fix: tightened to count only literal self-closing tags. The historical XML comments mentioning `DockRobot` at lines 84, 123, 463, 481, 503, 515, 518, 520 are PRESERVED as documentation of the deprecated API — they are NOT regressions.)
    - Sanity: `grep -c "DockRobot" ros2/src/mowgli_behavior/trees/main_tree.xml` returns >= 8 (the 8 historical comments — acceptable; not a failure signal).
    - `grep -c "ApproachDock" ros2/src/mowgli_behavior/trees/main_tree.xml` >= 6 (one per former DockRobot site).
    - `grep -c "FineDock" ros2/src/mowgli_behavior/trees/main_tree.xml` >= 6.
    - `grep -c "PreUndockClearanceCheck" ros2/src/mowgli_behavior/trees/main_tree.xml` == 1 (one per UndockSequence).
    - `grep -c "PostUndockRtkValidation" ros2/src/mowgli_behavior/trees/main_tree.xml` == 1.
    - `grep -c "RecordDockApproachPose" ros2/src/mowgli_behavior/trees/main_tree.xml` == 1.
    - `colcon build --packages-select mowgli_behavior` exits 0.
    - Existing BT tests still pass: `colcon test --packages-select mowgli_behavior --ctest-args -E docking_nodes` reports >= prior test count, 0 failed (regression coverage of non-docking tests).
    - XML parses cleanly: `timeout 5 ros2 run mowgli_behavior behavior_tree_node` does NOT emit "BT.CPP" parse error in the first 3 seconds of stderr.
    - Plan 02-06's docking_nodes tests still pass: `colcon test --ctest-args -R docking_nodes` reports 12 tests passing.
  </acceptance_criteria>
  <done>SPEC R-10 acceptance met (zero DockRobot calls). UndockSequence has clearance check + RTK validation + RecordDockApproachPose. R-9 OuterReactiveSequence behavior verified or explicitly guarded. Plan 02-08 hardware acceptance can run.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: GUI Dock-card extension — useDockMatch hook + DockMatchCard component + topicMap entries (D-09/D-10/D-11/D-12)</name>
  <read_first>
    - gui/web/src/hooks/useMagYaw.ts (FULL — verbatim template for useDockMatch)
    - gui/web/src/hooks/useDockingSensor.ts (similar pattern; useful for the bool-gate idiom)
    - gui/web/src/types/ros.ts (Plan 02-01 — verify DockMatchConfidence type is present and field names are snake_case)
    - gui/web/src/components/MowerStatus.tsx (existing dock card site — identify the JSX render order; DockMatchCard goes near the existing Charging indicator per D-09)
    - gui/web/src/components/settings/DockingSection.tsx (existing dock settings — identify whether DockMatchCard is its peer or replaces a sub-section)
    - gui/pkg/providers/ros.go lines 25-60 (topicMap definition — this is where the 2 new entries go)
    - gui/pkg/api/mowglinext.go line 253 (dispatch case pattern — mirror for the 2 new keys)
    - .planning/phases/02-lidar-dock-pose-estimation/02-CONTEXT.md D-09 (extend EXISTING Dock-card, not new card), D-10 (status badge + numeric pair color thresholds), D-11 (Recapture confirm-modal), D-12 (Go-relay topicMap pattern)
    - .planning/phases/02-lidar-dock-pose-estimation/02-RESEARCH.md §Pitfall 4 (D-12 reconciliation: rosbridge_server is absent in this fork; topicMap is the established pattern)
    - .planning/phases/01-coverage-planner-rewrite/01-04-PLAN.md (Phase 1 GUI patterns — useCoveragePlan hook + EditAreaModal modal pattern; Recapture confirm modal mirrors EditAreaModal style)
  </read_first>
  <files>
    gui/pkg/providers/ros.go (MODIFY — 2-line topicMap extension)
    gui/pkg/api/mowglinext.go (MODIFY — 2 dispatch cases)
    gui/web/src/hooks/useDockMatch.ts (NEW — mirrors useMagYaw)
    gui/web/src/components/DockMatchCard.tsx (NEW — Dock-card extension component)
    gui/web/src/components/MowerStatus.tsx (MODIFY — render DockMatchCard near Charging indicator)
  </files>
  <behavior>
    **gui/pkg/providers/ros.go**: append to topicMap:
    ```go
    "dockMatchPose":       {"/dock_match/pose", "geometry_msgs/msg/PoseWithCovarianceStamped"},
    "dockMatchConfidence": {"/dock_match/confidence", "mowgli_interfaces/msg/DockMatchConfidence"},
    ```

    **gui/pkg/api/mowglinext.go**: at the existing dispatch switch (line 253 area, where each topicMap key has a case), add:
    ```go
    case "dockMatchPose":
        // route through generic Pose subscription
        return providers.SubscriberRouteJSON(w, r, ros, "dockMatchPose")
    case "dockMatchConfidence":
        return providers.SubscriberRouteJSON(w, r, ros, "dockMatchConfidence")
    ```
    (Pattern verbatim from the existing magYaw or dockingSensor case; do NOT invent new dispatch idioms.)

    **useDockMatch.ts**: mirror useMagYaw.ts. Subscribes to two topics; returns `{pose, confidence, lastMessageAt}`:
    ```typescript
    import {useEffect, useRef, useState} from "react";
    import {DockMatchConfidence, PoseWithCovarianceStamped} from "../types/ros.ts";
    import {useWS} from "./useWS.ts";

    export const useDockMatch = (): {
      pose: PoseWithCovarianceStamped | null;
      confidence: DockMatchConfidence | null;
      lastMessageAt: number | null;
    } => {
      const [pose, setPose] = useState<PoseWithCovarianceStamped | null>(null);
      const [confidence, setConfidence] = useState<DockMatchConfidence | null>(null);
      const lastRef = useRef<number | null>(null);
      const [lastMessageAt, setLastMessageAt] = useState<number | null>(null);

      const poseStream = useWS<string>(/*...*/);
      const confStream = useWS<string>(/*...*/);

      useEffect(() => {
        poseStream.start("/api/mowglinext/subscribe/dockMatchPose");
        confStream.start("/api/mowglinext/subscribe/dockMatchConfidence");
        return () => { poseStream.stop(); confStream.stop(); };
      }, []);

      return {pose, confidence, lastMessageAt};
    };
    ```
    Use the same parse-and-record idiom as useMagYaw.ts; in confStream's onMessage callback, also update lastMessageAt for staleness UI.

    **DockMatchCard.tsx**: a card component rendered in the MowerStatus dock area:
    - Confidence badge per D-10:
      - GREEN: confidence.trusted == true (and inlier >= 0.70 AND rmse <= 0.05)
      - YELLOW: 0.60 <= inlier < 0.70 OR 0.05 < rmse <= 0.10 (border zone)
      - RED: inlier < 0.60 OR rmse > 0.10 OR !trusted
      - GRAY: lastMessageAt is null (no data yet) or `Date.now() - lastMessageAt > 5000` (stale)
    - Below the badge: text `Inlier 78% / RMSE 3.2 cm` (format: `Inlier ${(conf.inlier_ratio * 100).toFixed(0)}% / RMSE ${(conf.rmse_m * 100).toFixed(1)} cm`).
    - Below that: `Scan age: —` placeholder (DEFERRED to Phase 3 polishing per WARNING 4 fix — Plan 02-08 files_modified does NOT wire the dock_scan_meta-age endpoint; this is locked option (b) of the WARNING 4 disposition. Add a TODO code comment: `// TODO(phase-3): wire dock_scan_meta.captured_at age via Go relay`).
    - Below that: `Last fine-dock lateral: —` placeholder (DEFERRED to Phase 3 polishing per WARNING 4 fix — sourced from mow_session_monitor JSONL but the GUI cross-reads JSONL from disk are out of scope for Phase 2; the data is already captured by Plan 02-08's mow_session_monitor extension, but exposing it to the GUI requires a new Go endpoint that is intentionally NOT in Plan 02-08 files_modified. Add a TODO code comment: `// TODO(phase-3): wire lateral_error_at_contact via /api/mowglinext/sessions/last/lateral_error endpoint`).
    - Recapture button per D-11: triggers a confirm-modal with the German text from CONTEXT.md ("Capture wird den aktuellen LiDAR-Snapshot als neuen dock_scan.pcd speichern. Roboter sollte sicher auf dem Dock sitzen. Fortfahren?"). On confirm, POST to the existing `/calibrate_imu_yaw_node/calibrate` service (or the existing GUI helper that triggers it — read DockingSection.tsx for the existing calibrate-button wiring; reuse that exact endpoint).

    **MowerStatus.tsx**: render `<DockMatchCard />` in the existing dock-status area (per D-09 "extend the existing Dock-card, NOT a new card"). Locate where the Charging indicator is rendered and add DockMatchCard immediately below it (or above; operator preference — pick one and document).

    The card UI does NOT block any existing functionality; if /dock_match/* is absent (Plan 02-04 not deployed yet), the card shows GRAY badge + "—" placeholders and the Recapture button still works (it triggers the existing calibrate flow).
  </behavior>
  <action>
1. **gui/pkg/providers/ros.go**: locate the topicMap (line 29 area). Append the 2 entries alphabetically (between `diagnostics` and `emergency` if alphabetical, or at the dock-related cluster if grouped). The MsgType strings MUST exactly match the auto-generated foxglove_bridge expectations: `geometry_msgs/msg/PoseWithCovarianceStamped` and `mowgli_interfaces/msg/DockMatchConfidence`.

2. **gui/pkg/api/mowglinext.go** line ~253: add the 2 dispatch cases. Read the existing magYaw case carefully — if dispatch uses a generic helper (likely yes), the new cases are just 2 string-equality lines. If dispatch is bespoke per topic (less likely), mirror the bespoke pattern.

3. **useDockMatch.ts**: copy useMagYaw.ts verbatim, rename function, add the second WS stream. Test that the file compiles: `cd gui/web && yarn lint` should pass.

4. **DockMatchCard.tsx**: build the card component. Use the existing design-system Tailwind / component primitives (read other cards in the same dir to identify them). For the confirm modal, mirror EditAreaModal.tsx (Plan 01-04 outputs) exactly — same Modal primitive, same confirm-button layout, same i18n approach.

5. **MowerStatus.tsx**: add `import {DockMatchCard} from './DockMatchCard';` at the top. Render `<DockMatchCard />` in the dock-related section. Keep all other layout intact.

6. Run the GUI build to confirm no TS or React errors:
```bash
cd gui/web && yarn build 2>&1 | tail -20
cd gui && go build ./... 2>&1 | tail -10
```
Both must exit 0.

CRITICAL — per memory `feedback_language.md`: the German confirm-modal text is operator-facing in chat-style; the rest of the UI labels are English (operator chose to mix). Don't translate the existing card labels.

CRITICAL — per CONTEXT.md D-12 reconciliation: do NOT introduce `@foxglove/ws-protocol` or `roslib` as a new browser-side dependency. Plan 01-04 did add `roslib@^2.x` for direct rosbridge actions, but D-12 explicitly locks the topic-map Go-relay pattern for /dock_match/*. The card MUST NOT spin up a parallel WebSocket connection.

CRITICAL — per CONTEXT.md D-11: the Recapture button MUST go through a confirm modal. Do not allow a single-click misclick to overwrite dock_scan.pcd.
  </action>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/mowglinext-danyial && grep -c "dockMatchPose\|dockMatchConfidence" gui/pkg/providers/ros.go && grep -c "dockMatchPose\|dockMatchConfidence" gui/pkg/api/mowglinext.go && cd gui/web && yarn build 2>&1 | tail -10 && cd .. && go build ./... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - topicMap has both entries: `grep -c "dockMatchPose\|dockMatchConfidence" gui/pkg/providers/ros.go` >= 2.
    - Dispatch cases present: `grep -c "case \"dockMatchPose\"\|case \"dockMatchConfidence\"" gui/pkg/api/mowglinext.go` >= 2.
    - No new browser-side ros library dependency added: `! diff <(git show HEAD:gui/web/package.json | grep -E "@foxglove/ws-protocol|^\s*\"roslib") <(grep -E "@foxglove/ws-protocol|^\s*\"roslib" gui/web/package.json)` succeeds (no new entries beyond what was there before this plan).
    - File gui/web/src/hooks/useDockMatch.ts exists; `grep -q "export const useDockMatch" gui/web/src/hooks/useDockMatch.ts` succeeds.
    - File gui/web/src/components/DockMatchCard.tsx exists; `grep -q "export.*DockMatchCard" gui/web/src/components/DockMatchCard.tsx` succeeds; `grep -q "Inlier" gui/web/src/components/DockMatchCard.tsx` succeeds; `grep -q "RMSE" gui/web/src/components/DockMatchCard.tsx` succeeds; `grep -q "trusted" gui/web/src/components/DockMatchCard.tsx` succeeds.
    - Recapture confirm modal text present: `grep -q "Capture wird den aktuellen LiDAR-Snapshot" gui/web/src/components/DockMatchCard.tsx` succeeds.
    - MowerStatus.tsx renders DockMatchCard: `grep -q "DockMatchCard" gui/web/src/components/MowerStatus.tsx` succeeds.
    - `yarn build` (gui/web) exits 0.
    - `go build ./...` (gui) exits 0.
  </acceptance_criteria>
  <done>D-09 (extend dock card), D-10 (badge + numerics), D-11 (confirm modal), D-12 (Go-relay topicMap) all satisfied. Operator can see live ICP confidence on the GUI; dock_scan_match degraded mode shows GRAY badge cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BT XML schema -> behavior_tree_node parser | Bad XML crashes the BT system; Plan 02-06 factory registrations must match XML node names exactly |
| GUI Recapture button -> /calibrate_imu_yaw_node/calibrate | Operator-confirmed click triggers a 30s+ calibration drive |
| topicMap relay -> browser | New keys deliver msg JSON to anyone connected to the Go API |
| BT node naming -> factory registration | Typo in XML name causes runtime "node not registered" error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | DoS | Bad XML edit prevents behavior_tree_node from starting | mitigate | Task 1 verify step explicitly checks node startup; gtest 12 cases from Plan 02-06 still pass; Plan 02-08 sim test covers end-to-end. |
| T-07-02 | Tampering | Migration misses one of the 6 DockRobot sites | mitigate | grep -c == 0 acceptance check is hard gate. |
| T-07-03 | DoS | E-Stop reset auto-restarts FineDock unintentionally | mitigate | OuterReactiveSequence verification in Task 1 documents the latch (existing or added). Operator must explicitly re-issue COMMAND_HOME. |
| T-07-04 | Tampering | Operator misclicks Recapture and corrupts dock_scan.pcd | mitigate | D-11 confirm modal; capture itself is gated on is_charging + stationarity (Plan 02-03). |
| T-07-05 | Spoofing | Browser-side adversary publishes fake /dock_match/confidence | accept | Same trust as the Go API (operator-owned); no PII; UI is read-only display. |
| T-07-06 | Information Disclosure | Confidence values leak through topicMap to anyone with API access | accept | Operator-owned local network; same trust as /odometry/filtered_map (already exposed). |
| T-07-07 | Tampering | UndockSequence Pre/Post insertion at wrong order breaks safety net | mitigate | Task 1 acceptance grep verifies all three new BT nodes appear in main_tree.xml; Plan 02-08 sim test covers the runtime sequence. |
</threat_model>

<verification>
- main_tree.xml has zero DockRobot calls (R-10)
- All 6 former sites contain ApproachDock+FineDock subtree
- UndockSequence has PreUndockClearanceCheck before BackUp + PostUndockRtkValidation after CalibrateHeadingFromUndock + RecordDockApproachPose at end
- behavior_tree_node parses the migrated XML without error
- Plan 02-06's docking_nodes tests still pass
- topicMap has dockMatchPose + dockMatchConfidence entries
- useDockMatch hook + DockMatchCard component compile
- yarn build + go build exit 0
- No new browser-side ros library dependency (D-12 reconciliation)
- D-11 Recapture confirm-modal text matches CONTEXT.md verbatim
</verification>

<success_criteria>
- SPEC R-9 satisfied: OuterReactiveSequence prevents auto-restart of FineDock after E-Stop reset (existing latch verified or new guard added)
- SPEC R-10 satisfied: zero `<DockRobot ` literal tags (BLOCKER 1 — counts tags only, not the 8 historical comment occurrences); 6 sites migrated to LidarDock subtree
- D-09 satisfied: DockMatchCard extends the existing dock area, NOT a separate card
- D-10 satisfied: GREEN/YELLOW/RED badge + Inlier_% / RMSE_cm pair
- D-11 satisfied: Recapture button + confirm modal with the locked German text
- D-12 satisfied: Go-relay topicMap pattern; no new browser-side dependency
- Pitfall 4 mitigated: D-12 literal interpretation reconciled to topicMap idiom
- UndockSequence carries the new safety nets (R-12 + R-13) plus the post-undock pose persistence (R-5)
</success_criteria>

<output>
After completion, create `.planning/phases/02-lidar-dock-pose-estimation/02-07-SUMMARY.md` describing: each of the 6 DockRobot sites and what wrapper they were embedded in (CriticalBatteryDock, RainDockAndResume, BatteryDockAndResume, FailedCoverageDock, plus the 2 inside ReactiveFallback patterns); the UndockSequence diff (3 nodes inserted); the R-9 OuterReactiveSequence verification verdict (existing latch sufficient OR new explicit guard added); the GUI Dock-card before/after; and the Plan 02-08 prerequisites (sim publisher + e2e_test phase + mow_session_monitor).
</output>
