---
phase: 02-lidar-dock-pose-estimation
plan: 08
type: execute
wave: 5
depends_on: [03, 04, 05, 06, 07]
files_modified:
  - ros2/src/mowgli_simulation/scripts/synthetic_scan_kicp_publisher.py
  - ros2/src/mowgli_simulation/CMakeLists.txt
  - ros2/src/mowgli_simulation/launch/sim_lidar_docking.launch.py
  - ros2/src/e2e_test.py
  - ros2/scripts/mow_session_monitor.py
  - .planning/phases/02-lidar-dock-pose-estimation/02-VERIFICATION.md
  - .planning/phases/02-lidar-dock-pose-estimation/02-08-PI5-CHECKLIST.md
autonomous: false
requirements: [R-7]
must_haves:
  truths:
    - "synthetic_scan_kicp_publisher.py publishes /scan_kicp matching the sim-dock geometry (D-14)"
    - "sim_lidar_docking.launch.py composes mowgli_lidar_docking + synthetic publisher + behavior_tree_node so end-to-end FineDock can be exercised in headless sim"
    - "e2e_test.py gains _run_fine_dock_phase that places the sim robot 1.5 m before the dock + asserts is_charging within 60 s"
    - "mow_session_monitor.py logs /dock_match/pose, /dock_match/confidence, lateral_error_at_contact per cycle (D-15)"
    - "02-VERIFICATION.md documents R-1..R-13 acceptance evidence with pass/fail per requirement"
    - "02-08-PI5-CHECKLIST.md is the operator-gated 5-of-5 hardware acceptance checklist (autonomous: false; mirrors Phase 1 SPEC AC-13 pattern)"
    - "Phase 2 SUMMARY explicitly states 'automatable scope COMPLETE; T3 hardware checkpoint pending operator' until Pi5 verifies"
  artifacts:
    - path: "ros2/src/mowgli_simulation/scripts/synthetic_scan_kicp_publisher.py"
      provides: "Synthetic /scan_kicp publisher matching sim-dock geometry; replaces real LD19 in headless sim"
      contains: "synthetic_scan_kicp_publisher"
    - path: "ros2/src/e2e_test.py"
      provides: "extended e2e harness — _run_fine_dock_phase asserts FineDock convergence in sim"
      contains: "_run_fine_dock_phase"
    - path: "ros2/scripts/mow_session_monitor.py"
      provides: "Per-sample JSONL logger with new dock_match.* + lateral_error_at_contact fields"
      contains: "dock_match"
    - path: ".planning/phases/02-lidar-dock-pose-estimation/02-VERIFICATION.md"
      provides: "R-1..R-13 acceptance matrix with evidence pointers"
      contains: "R-1"
    - path: ".planning/phases/02-lidar-dock-pose-estimation/02-08-PI5-CHECKLIST.md"
      provides: "Operator-gated 5-of-5 hardware procedure"
      contains: "Pi5"
  key_links:
    - from: "ros2/src/mowgli_simulation/scripts/synthetic_scan_kicp_publisher.py"
      to: "/scan_kicp"
      via: "rclpy publisher (SensorDataQoS)"
      pattern: "/scan_kicp"
    - from: "ros2/src/e2e_test.py"
      to: "FineDock + ApproachDock BT subtree"
      via: "rclpy ActionClient<NavigateToPose> probe + sim teleport + is_charging gate"
      pattern: "_run_fine_dock_phase"
    - from: "ros2/scripts/mow_session_monitor.py"
      to: "/dock_match/pose + /dock_match/confidence"
      via: "rclpy subscribers + JSONL row append"
      pattern: "dock_match"
---

<objective>
Close out Phase 2 by delivering test infrastructure + verification artifacts:

1. **D-14 sim publisher**: a `synthetic_scan_kicp_publisher.py` rclpy node publishes `/scan_kicp` matching the simulated dock geometry, so dock_scan_capture (Plan 02-03) and dock_scan_match (Plan 02-04) can exercise their full pipelines in headless Gazebo without a real LiDAR.
2. **Sim launch composition**: a new `sim_lidar_docking.launch.py` glues the synthetic publisher + dock_scan_match + behavior_tree_node + Gazebo world so a single command stands up the end-to-end FineDock pipeline in CI.
3. **e2e_test.py extension**: a new `_run_fine_dock_phase` Python method that teleports the sim robot to ~1.5 m before the dock, triggers a dock cycle, and asserts `is_charging` within 60 s. Pre-validates SPEC R-7 in CI before hardware.
4. **D-15 session monitor extension**: `mow_session_monitor.py` logs `/dock_match/pose`, `/dock_match/confidence`, and `lateral_error_at_contact` per cycle so committed JSONLs from the operator's 5-of-5 hardware runs become diff-able evidence.
5. **02-VERIFICATION.md**: an R-1..R-13 acceptance matrix with pointers to the gtest IDs / sim test phases / hardware checklist items that prove each requirement.
6. **02-08-PI5-CHECKLIST.md**: the operator-gated 5-of-5 hardware acceptance procedure, mirroring Phase 1 SPEC AC-13's checklist pattern. THIS plan is `autonomous: false` because Task 3 (the hardware checkpoint) requires a Pi5 + RTK + outdoor garden — Claude cannot execute it.

Per CLAUDE.md "Pi5 hardware test before PR" memory and Phase 1 Plan 01-09's automatable-vs-hardware split: this plan delivers all the AUTOMATABLE scope; the Pi5 5-of-5 acceptance is operator-gated and gates Phase 2 closure.

Per RESEARCH §Pitfall 5: this plan also captures the Pi5 RegisterFrame latency baseline (Plan 02-01 may have deferred it — capture it here at the start of the hardware checkpoint).

Purpose: Without sim coverage, FineDock first sees the light of day on Pi5 hardware — too risky given safety-critical motion control. Without VERIFICATION.md + PI5-CHECKLIST.md, the phase has no operator-readable handoff document.
Output: Sim-coverage end-to-end FineDock cycle; mow_session_monitor JSONLs include /dock_match/* fields; operator has a step-by-step Pi5 checklist; phase verification matrix complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-lidar-dock-pose-estimation/02-SPEC.md
@.planning/phases/02-lidar-dock-pose-estimation/02-CONTEXT.md
@.planning/phases/02-lidar-dock-pose-estimation/02-RESEARCH.md
@.planning/phases/02-lidar-dock-pose-estimation/02-07-SUMMARY.md
@.planning/phases/01-coverage-planner-rewrite/01-09-SUMMARY.md
@.planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md
@.planning/phases/01-coverage-planner-rewrite/01-VERIFICATION.md
@CLAUDE.md
@.claude/rules/ros2.md

<interfaces>
Plans 02-03, 02-04, 02-06, 02-07 all rely on /scan_kicp existing at runtime. In sim, the LD19 driver is absent — synthetic_scan_kicp_publisher fills the gap.

Plan 02-04 dock_scan_match.yaml param defaults govern matcher behavior in sim too — pass them through `sim_lidar_docking.launch.py` unmodified.

Plan 02-03 dock_scan_capture writes the sim-side dock_scan.pcd into the `mowgli_sim_maps` Docker volume. Plan 02-04's dock_scan_match reads from the same volume after a sim calibration drive — the sim run must do a calibration first OR use a pre-baked sim-dock_scan.pcd.

Existing today (read these before editing):
- ros2/src/mowgli_simulation/CMakeLists.txt + package.xml — confirm ament_python style + scripts install pattern (the package may currently be ament_cmake with C++ — adapt accordingly)
- ros2/src/mowgli_simulation/launch/ — existing launch files; mirror their style for sim_lidar_docking.launch.py
- ros2/src/mowgli_simulation/worlds/ — Gazebo world files; identify the dock model + its pose so the synthetic publisher emits geometry consistent with it
- ros2/src/e2e_test.py — existing CI smoke test; locate the existing phase methods (Phase 1 added _run_coverage_phase or similar) and mirror that style for _run_fine_dock_phase
- ros2/scripts/mow_session_monitor.py — existing JSONL logger; locate the metadata header schema + the per-sample row schema to extend
- .planning/phases/01-coverage-planner-rewrite/01-09-SUMMARY.md "Hardware Checkpoint Procedure" — the existing Pi5 checklist style to mirror
- .planning/phases/01-coverage-planner-rewrite/01-VERIFICATION.md — the R-1..R-13 acceptance matrix style to mirror
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: synthetic_scan_kicp_publisher + sim_lidar_docking.launch.py + e2e_test.py extension</name>
  <read_first>
    - ros2/src/mowgli_simulation/CMakeLists.txt + package.xml (FULL — confirm package layout; this task creates the scripts/ subdirectory if missing)
    - ros2/src/mowgli_simulation/launch/ (any *.launch.py — mirror style)
    - ros2/src/mowgli_simulation/worlds/*.world (or .sdf) — identify the dock model placement (x, y, yaw); the synthetic publisher emits geometry consistent with that pose
    - ros2/src/e2e_test.py (FULL — locate existing phase-method pattern; identify how Phase 1's coverage phase asserts plan invariants)
    - ros2/src/mowgli_localization/scripts/kinematic_icp_scan_frame_relay.py (for the /scan_kicp QoS profile + frame_id contract — synthetic publisher must use the SAME frame_id `lidar_link_wheels` and SAME QoS)
    - .planning/phases/02-lidar-dock-pose-estimation/02-RESEARCH.md §Architecture Diagram (sim-side) — confirms what dock_scan_match expects
    - .planning/phases/02-lidar-dock-pose-estimation/02-CONTEXT.md D-14 (sim publisher locked spec)
  </read_first>
  <files>
    ros2/src/mowgli_simulation/scripts/synthetic_scan_kicp_publisher.py (NEW)
    ros2/src/mowgli_simulation/CMakeLists.txt (MODIFY — install the new script)
    ros2/src/mowgli_simulation/launch/sim_lidar_docking.launch.py (NEW)
    ros2/src/e2e_test.py (MODIFY — add _run_fine_dock_phase method + invocation)
    ros2/src/mowgli_simulation/test/test_synthetic_scan_publisher.py (NEW — pytest verifying the publisher emits coherent scan geometry)
  </files>
  <behavior>
    **synthetic_scan_kicp_publisher.py**:
    - rclpy Node `synthetic_scan_kicp_publisher`; subscribes to nothing; publishes /scan_kicp at 10 Hz (matching real LD19 cadence) with SensorDataQoS.
    - Reads ROS2 params: `dock_pose_x` (default 5.0), `dock_pose_y` (default 5.0), `dock_pose_yaw_rad` (default 0.0), `world_obstacles_yaml` (optional path to a JSON/YAML describing additional obstacle polygons in the sim world). The dock geometry is hardcoded into the publisher: a V-funnel at the dock pose (3 line segments approximating the YF500 dock structure — 0.4 m base, 0.6 m arms at ±20° from forward).
    - Subscribes to TF; on each tick, looks up `map -> base_footprint_wheels` (or queries Gazebo via /gazebo/get_model_state — choose the simpler path; TF is preferred since the sim already publishes the parallel TF tree).
    - For each tick, generates 360 ray-cast results from the robot's current pose against the dock V-funnel + any world obstacles. Returns r=range_max for misses, r=hit_distance for hits. Adds Gaussian noise (σ=0.005 m) to mimic LD19 measurement noise. NaN for rays beyond range_max.
    - Sets `header.frame_id = "lidar_link_wheels"`, `header.stamp = node->now()`. Sets angle_min, angle_max, angle_increment, range_min, range_max to LD19-compatible values (read from a constant block at the top of the file; document each value's source).
    - Publishes the LaserScan message.

    **sim_lidar_docking.launch.py**:
    - Composes: existing sim_full_system.launch.py (or sim_small_garden.launch.py — pick the one used by Phase 1's e2e_test) + synthetic_scan_kicp_publisher + dock_scan_match (via mowgli_lidar_docking) + behavior_tree_node (with the migrated main_tree.xml from Plan 02-07).
    - Pre-bakes a sim-dock_scan.pcd (committed to the repo at `ros2/src/mowgli_simulation/test_data/sim_dock_scan.pcd`) so dock_scan_match starts in non-degraded mode. Alternatively, the launch file orchestrates a sim-calibration step that calls dock_scan_capture before the rest of the stack starts — pick the simpler path (pre-baked PCD recommended for CI determinism).
    - Operator can run: `ros2 launch mowgli_simulation sim_lidar_docking.launch.py` and within ~30 s see /dock_match/pose flowing, /dock_match/confidence trusted=true.

    **e2e_test.py extension**:
    - Add `def _run_fine_dock_phase(self):` method to the test class. Mirror the existing _run_coverage_phase / _run_undock_phase pattern.
    - Step 1: assert dock_scan_match is publishing /dock_match/pose (poll for 5 s, fail test if no message arrives).
    - Step 2: teleport sim robot to (dock_x - 1.5*cos(dock_yaw), dock_y - 1.5*sin(dock_yaw), dock_yaw_rad) — i.e., 1.5 m back along the dock-approach line. Use the existing sim-teleport helper if Phase 1 added one; if not, call /gazebo/set_model_state.
    - Step 3: invoke COMMAND_HOME via the existing HighLevelControl service helper (Phase 1 e2e_test uses this for return-to-dock).
    - Step 4: poll for is_charging == true with timeout 60 s. If charging engages, assert latest /dock_match/pose lateral_error against the dock pose < 0.02 m (R-7 acceptance ≤ 2 cm). If 60 s pass without charging, fail the test with a diagnostic log capture.
    - Append `self._run_fine_dock_phase()` to the test runner's main flow (after _run_coverage_phase, after the existing return-to-dock phase).

    **test_synthetic_scan_publisher.py** (smoke unit test, NOT the full sim run):
    - Spin the publisher with mocked TF returning robot at known pose; capture one /scan_kicp message; assert frame_id == "lidar_link_wheels"; assert len(ranges) > 100 (i.e., the publisher actually emits a meaningful scan); assert at least one range value is between range_min and range_max (i.e., the dock V-funnel produces visible hits).
  </behavior>
  <action>
1. **synthetic_scan_kicp_publisher.py** (~250 lines): see <behavior>. Use a simple raycast: for each angle, compute the intersection between the ray (from robot in lidar_link_wheels frame) and each line segment of the dock V-funnel; take the minimum distance.

2. **sim_lidar_docking.launch.py**: include the existing sim_full_system.launch.py via IncludeLaunchDescription, then add the 2 new Node actions. Pre-bake the dock_scan.pcd: capture it once (e.g., from a manual sim run with dock_scan_capture, then commit `ros2/src/mowgli_simulation/test_data/sim_dock_scan.pcd` AND `sim_dock_scan_meta.yaml`); the launch file copies them into `/ros2_ws/maps/` at startup (or sets dock_scan_match params to read directly from the test_data path).

3. **e2e_test.py**: add the new method. Use rclpy + the existing helper utilities. The is_charging poll is the gate — extra assertions on /dock_match/pose lateral_error are bonus diagnostics.

4. **test_synthetic_scan_publisher.py**: standalone pytest; uses rclpy.init/shutdown idiom from Plan 02-03's tests.

5. CMakeLists.txt: install the new script + launch file. If the package layout is ament_python, the script goes in setup.py entry_points or scripts list.

CRITICAL — per CLAUDE.md "Architecture Invariants" #1: this synthetic publisher is sim-only. It does NOT publish in production. The launch file is named `sim_lidar_docking.launch.py` to make this explicit. If anyone tries to use it in production (Pi5 ARM64 deploy), the dock_scan_match would receive 2 conflicting /scan_kicp publishers — DDS pub/sub semantics would interleave them, breaking ICP. Document this explicitly in the launch file's docstring.

CRITICAL — per memory `feedback_pi5_test_then_pr.md`: sim coverage is necessary but NOT sufficient. The Pi5 hardware acceptance (Task 3) is the actual gate.
  </action>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/mowglinext-danyial/ros2 && colcon build --packages-select mowgli_simulation --event-handlers console_cohesion+ 2>&1 | tail -10 && colcon test --packages-select mowgli_simulation --pytest-args -k synthetic_scan --event-handlers console_cohesion+ 2>&1 | tail -10 && ls ros2/src/mowgli_simulation/launch/sim_lidar_docking.launch.py && grep -q "_run_fine_dock_phase" ros2/src/e2e_test.py && echo "ALL CHECKS OK"</automated>
  </verify>
  <acceptance_criteria>
    - File ros2/src/mowgli_simulation/scripts/synthetic_scan_kicp_publisher.py exists.
    - Synthetic publisher emits frame_id=lidar_link_wheels: `grep -q "lidar_link_wheels" ros2/src/mowgli_simulation/scripts/synthetic_scan_kicp_publisher.py` succeeds.
    - File ros2/src/mowgli_simulation/launch/sim_lidar_docking.launch.py exists; `grep -q "synthetic_scan_kicp_publisher\|dock_scan_match" ros2/src/mowgli_simulation/launch/sim_lidar_docking.launch.py` succeeds.
    - e2e_test.py has the new phase: `grep -q "def _run_fine_dock_phase" ros2/src/e2e_test.py` succeeds.
    - test_synthetic_scan_publisher.py exists with smoke tests.
    - `colcon build --packages-select mowgli_simulation` exits 0.
    - `colcon test --pytest-args -k synthetic_scan` reports >= 1 test passing.
  </acceptance_criteria>
  <done>D-14 sim publisher implemented; sim_lidar_docking.launch composes the full pipeline; e2e_test.py covers FineDock convergence in headless sim; smoke pytest pins the publisher contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: mow_session_monitor.py extension + 02-VERIFICATION.md</name>
  <read_first>
    - ros2/scripts/mow_session_monitor.py (FULL — locate the metadata header schema + per-sample row schema; the new fields go alongside existing ones)
    - .planning/phases/01-coverage-planner-rewrite/01-VERIFICATION.md (FULL — verbatim style template for 02-VERIFICATION.md)
    - .planning/phases/01-coverage-planner-rewrite/01-VALIDATION.md (FULL — same)
    - .planning/phases/02-lidar-dock-pose-estimation/02-RESEARCH.md §Validation Architecture (R-1..R-13 phase-requirements-to-test map; this is the source for 02-VERIFICATION.md)
    - .planning/phases/02-lidar-dock-pose-estimation/02-CONTEXT.md D-15 (mow_session_monitor extension locked spec)
    - CLAUDE.md "Mowing Session Monitoring" section (the existing JSONL contract + what each field means)
  </read_first>
  <files>
    ros2/scripts/mow_session_monitor.py (MODIFY — add /dock_match/* subscribers + new fields per sample + new summary fields)
    .planning/phases/02-lidar-dock-pose-estimation/02-VERIFICATION.md (NEW)
  </files>
  <behavior>
    **mow_session_monitor.py extensions** (D-15):
    - Add subscribers for /dock_match/pose (PoseWithCovarianceStamped) and /dock_match/confidence (DockMatchConfidence). Same pattern as existing pose/twist/state subscribers — store latest, sample at the existing 10 Hz cadence.
    - Per-sample new fields:
      - `dock_match.pose.x`, `.y`, `.yaw` (extracted from latest /dock_match/pose; null if no msg yet)
      - `dock_match.confidence.inlier_ratio`, `.rmse_m`, `.trusted`
      - `dock_match.staleness_ms` (now - last /dock_match/pose received; for diagnosing R-3 1s-abort triggers)
      - `lateral_error_at_contact_m` (computed only at the moment is_charging transitions false->true; null otherwise — this is the R-7 ground-truth measurement; computed as the projection of (robot_pose - dock_anchor) onto the dock-frame y-axis at first contact)
    - Summary record (last line of JSONL) gets:
      - `lateral_errors_at_contact_m` (list of all is_charging-rising-edge values from the session)
      - `mean_lateral_error_at_contact_m` (mean of the list; useful as a session aggregate)
      - `dock_match_trusted_pct` (% of samples where trusted == true; sanity gauge for matcher health)

    **02-VERIFICATION.md** structure (mirror Phase 1 style; ~3-4 KB):
    - Header: phase + automated test commands + manual hardware steps
    - R-1..R-13 acceptance matrix:
      | Req | Description | Test | Status | Evidence |
      |-----|-------------|------|--------|----------|
      | R-1 | dock_scan.pcd capture | Plan 02-03 pytest test_capture_writes_pcd_and_meta | UNVERIFIED | — |
      | R-2 | /dock_match/pose >= 5 Hz | Plan 02-04 gtest test_node_publishes_at_advertised_rate | UNVERIFIED | — |
      | R-3 | trust threshold | Plan 02-04 gtest test_corrupted_scan_drops_trust_within_1s | UNVERIFIED | — |
      | R-4 | cascade priority | Plan 02-05 pytest test_cascade_* (7 cases) | UNVERIFIED | — |
      | R-5 | RecordDockApproachPose | Plan 02-06 gtest test_RecordDockApproachPose* | UNVERIFIED | — |
      | R-6 | ApproachDock | Plan 02-06 gtest test_ApproachDock* | UNVERIFIED | — |
      | R-7 | FineDock convergence | Plan 02-08 sim test _run_fine_dock_phase + Pi5 5-of-5 | UNVERIFIED | — |
      | R-8 | confidence-loss abort | Plan 02-06 gtest test_FineDockAbortsOnConfidenceLoss | UNVERIFIED | — |
      | R-9 | E-Stop hold | Plan 02-06 gtest test_FineDockHaltsOnEmergency + Plan 02-07 OuterReactiveSequence verification + Pi5 5-of-5 | UNVERIFIED | — |
      | R-10 | DockRobot migrated | Plan 02-07 acceptance grep -c "DockRobot" == 0 | UNVERIFIED | — |
      | R-11 | auto-refresh policy | Plan 02-06 gtest test_RecordDockApproachPoseAutoRefresh* (2 cases) | UNVERIFIED | — |
      | R-12 | PreUndockClearanceCheck | Plan 02-06 gtest test_PreUndockClearance* (2 cases) | UNVERIFIED | — |
      | R-13 | PostUndockRtkValidation | Plan 02-06 gtest test_PostUndockRtkValidationThresholds | UNVERIFIED | — |
      | E2E sim | full FineDock phase | Plan 02-08 e2e_test _run_fine_dock_phase | UNVERIFIED | — |
      | Pi5 5-of-5 | hardware acceptance | Plan 02-08 PI5-CHECKLIST.md | UNVERIFIED | — |

    Status entries are UNVERIFIED at file-write time. Each will become VERIFIED with a commit hash + log link when the corresponding test/checkpoint actually passes (a separate `/gsd-verify-phase 2` invocation flips them).

    The matrix gives operator a single source of truth for "what's left to prove before Phase 2 is closed".
  </behavior>
  <action>
1. mow_session_monitor.py: locate the existing subscriber-setup block (around the metadata-header writeup) and add 2 new subscribers per the <behavior> spec. Locate the per-sample row builder and add the new fields. Locate the summary builder (Ctrl-C handler) and add the new aggregate fields. Be sure to update the metadata-header line at the start of the JSONL with the new schema-version field if there is one (or document the new fields in a comment block).

2. is_charging-rising-edge detection: keep a `prev_is_charging` field; on this-sample is_charging==true AND prev==false, capture the lateral_error_at_contact value. Compute it via the projection formula in <behavior>. This is a one-shot per cycle; the next is_charging cycle (after operator triggers undock + re-dock) will trigger another capture.

3. 02-VERIFICATION.md: write the matrix; populate Evidence column with placeholder `—` (the verification step in `/gsd-verify-phase 2` flips them to commit hashes + log line pointers).

CRITICAL — per CLAUDE.md "Mowing Session Monitoring": the JSONL is committed to git for notable runs. The new fields MUST NOT bloat the per-sample size dramatically — `dock_match.pose.{x,y,yaw}` + 3 confidence fields + 1 staleness + 1 lateral_error = 8 new floats per sample, ~120 bytes/sample at 10 Hz = ~3.6 KB/min added. Acceptable.
  </action>
  <verify>
    <automated>cd /Users/danny.smolinsky/jam-dev/mowglinext-danyial && grep -c "dock_match" ros2/scripts/mow_session_monitor.py >= 5 && grep -q "lateral_error_at_contact" ros2/scripts/mow_session_monitor.py && grep -q "R-1\|R-13" .planning/phases/02-lidar-dock-pose-estimation/02-VERIFICATION.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - mow_session_monitor.py has dock_match subscriptions: `grep -c "/dock_match/" ros2/scripts/mow_session_monitor.py` >= 2.
    - lateral_error_at_contact field present: `grep -q "lateral_error_at_contact" ros2/scripts/mow_session_monitor.py` succeeds.
    - dock_match_trusted_pct in summary: `grep -q "dock_match_trusted_pct" ros2/scripts/mow_session_monitor.py` succeeds.
    - File 02-VERIFICATION.md exists.
    - Matrix covers R-1 through R-13: `grep -c "^| R-" .planning/phases/02-lidar-dock-pose-estimation/02-VERIFICATION.md` >= 13.
    - E2E + Pi5 rows present: `grep -q "E2E sim\|Pi5 5-of-5" .planning/phases/02-lidar-dock-pose-estimation/02-VERIFICATION.md` succeeds.
  </acceptance_criteria>
  <done>D-15 monitor extension live; 02-VERIFICATION.md documents acceptance matrix with R-1..R-13 + E2E + hardware row.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: HUMAN — Pi5 hardware 5-of-5 acceptance in Eichenau garden (SPEC R-7 + R-9 + R-11 hardware gate)</name>
  <files>
    .planning/phases/02-lidar-dock-pose-estimation/02-08-PI5-CHECKLIST.md (NEW — operator-readable per how-to-verify step 5)
    .planning/phases/02-lidar-dock-pose-estimation/02-08-PI5-RESULTS.md (operator-created at end with one row per cycle)
  </files>
  <action>Operator follows the checklist in PI5-CHECKLIST.md (deliverable in step 5 of how-to-verify) on the Pi5 test bench in the Eichenau garden. Five consecutive end-to-end mow cycles must succeed at first attempt; one cycle must verify R-9 manual E-Stop semantics; one cycle must verify R-11 auto-refresh. Verdict committed to PI5-RESULTS.md. Re-run /gsd-verify-phase 2 afterwards to flip the R-7/R-9/R-11 matrix rows.</action>
  <verify>
    <automated>MISSING — operator-gated; after operator commits PI5-RESULTS.md, automation can grep for "5 of 5 PASS" and an explicit E-Stop result + R-11 auto-refresh result. Until then, this verify entry is intentionally a manual checkpoint.</automated>
  </verify>
  <done>Operator commits 02-08-PI5-RESULTS.md with all 5 cycles passing AND the R-9 manual E-Stop verification AND the R-11 auto-refresh verification. /gsd-verify-phase 2 then flips the VERIFICATION.md matrix rows.</done>
  <what-built>Operator-gated 5-of-5 hardware acceptance on Pi5 in Eichenau garden under RTK-Fixed conditions. Mirrors Phase 1 SPEC AC-13 pattern. THIS task is `autonomous: false` — Claude cannot SSH-drive the robot through 5 mow cycles outdoors; the operator runs the test bench and reports the verdict. Phase 2 closes when this task reports 5/5 SUCCESS.</what-built>
  <how-to-verify>
1. **Pre-flight (before driving outdoors):**
   - On the Pi5 test bench (`pi@10.10.40.68` per memory `reference_pi5_testbench.md`): pull the merged feat/mag-pipeline-resurrect branch (or whichever branch lands Phase 2). Run `make build` and confirm green.
   - Confirm `ls /ros2_ws/maps/dock_calibration.yaml /ros2_ws/maps/dock_scan.pcd /ros2_ws/maps/dock_scan_meta.yaml` shows all three files (per Plan 02-03 SPEC R-1: dock-yaw calibration GUI button produces them). If dock_scan.pcd is missing, run the calibration first via the GUI.
   - On the Pi5: start the system. Within 30 s, `ros2 topic echo /dock_match/confidence -n 1` MUST show `trusted: true` (or `trusted: false` with a clear inlier/rmse reason — capture the ros2 echo output to the SUMMARY).
   - Capture Pi5 RegisterFrame latency baseline (RESEARCH Pitfall 5): `ros2 topic hz /dock_match/pose` for 30 s; record mean rate to PI5-CHECKLIST. MUST be >= 5 Hz (SPEC R-2 minimum).

2. **5-of-5 mow cycle execution:**
   For each of 5 consecutive runs (operator drives the schedule):
   - Cycle N, step 1: from dock, send COMMAND_START via the GUI. Confirm UndockSequence executes through PreUndockClearanceCheck (rear clearance log line) + BackUp + CalibrateHeadingFromUndock + PostUndockRtkValidation (RTK error log line) + RecordDockApproachPose (dock_approach.yaml mtime updated).
   - Step 2: robot mows the small test area. Standard mow run; nothing dock-specific.
   - Step 3: when battery low (or operator triggers COMMAND_HOME early), confirm ApproachDock dispatches NavigateToPose to the dock_approach.yaml pose; confirm robot arrives within 60 s + tolerance 10 cm.
   - Step 4: confirm FineDock crawls forward + reaches is_charging on first attempt. **R-7 hard gate**: lateral error at contact MUST be ≤ 2 cm AND yaw error ≤ 1° (measured from /dock_match/pose at the moment of first is_charging).
   - Step 5: in parallel, mow_session_monitor.py runs from a separate SSH session (per CLAUDE.md "Whenever a mowing test is run") and captures the JSONL. Operator commits the JSONL after each cycle.

3. **Acceptance:**
   - 5 of 5 cycles MUST succeed at first attempt (R-7 hardware gate from SPEC AC).
   - At least 1 of the 5 must demonstrate the auto-refresh trigger (R-11) by being the first cycle after >= 7 days since prior dock_scan.pcd capture; verify dock_scan_meta.yaml.captured_at updates.
   - Operator manually injects E-Stop during one FineDock crawl (cycle 3 recommended) to verify R-9: motion stops within firmware deadline; after ResetEmergency the robot does NOT auto-restart docking — operator re-issues COMMAND_HOME and ApproachDock+FineDock cycle starts fresh.

4. **Reporting:**
   - For each cycle, capture: timestamp, BT log lines for the 8 new BT-node firings, mow_session_monitor JSONL, /dock_match/confidence at contact, lateral_error_at_contact, yaw_error_at_contact.
   - Aggregate into `.planning/phases/02-lidar-dock-pose-estimation/02-08-PI5-RESULTS.md` (operator creates this file at the end with a row per cycle).
   - Re-run `/gsd-verify-phase 2` from the dev container after Pi5 verdict — flips R-7, R-9, R-11 hardware gates to VERIFIED.

5. **02-08-PI5-CHECKLIST.md (deliverable in this task)**: write the operator-readable checklist file with:
   - Pre-flight steps (3 above)
   - Per-cycle steps (5 cycles × 5 steps)
   - Acceptance criteria (5/5 first-attempt + R-9 manual E-Stop + R-11 auto-refresh)
   - Reporting template
  </how-to-verify>
  <resume-signal>Operator commits .planning/phases/02-lidar-dock-pose-estimation/02-08-PI5-RESULTS.md with all 5 cycles passing (or documents failure modes for re-tuning). Then re-run /gsd-verify-phase 2 to flip the matrix entries.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sim publisher -> dock_scan_match | Synthetic LiDAR feeds the production matcher in CI; risk of false positives if sim geometry diverges from real LD19 |
| e2e_test.py teleport -> sim robot | Test harness controls Gazebo state; CI-only, no production exposure |
| mow_session_monitor JSONL -> committed git history | Operator-readable evidence for hardware acceptance; trust same as the Pi5 itself |
| Pi5 hardware (operator-driven) -> safety-critical motion | The actual robot with spinning blades; operator presence + collision_monitor + firmware safety latch are the active defences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Synthetic publisher emits geometry diverging from real LD19, hiding bugs that surface only on Pi5 | mitigate | Hardware 5-of-5 acceptance is the ground-truth gate; sim coverage is necessary but explicitly NOT sufficient (documented in PI5-CHECKLIST.md). |
| T-08-02 | DoS | Sim is_charging never engages despite FineDock running -> e2e_test hangs 60s | accept | Test timeout = 60s, fails cleanly with diagnostic; CI catches it. |
| T-08-03 | Tampering | Operator skips one of the 5 cycles | mitigate | PI5-CHECKLIST requires per-cycle JSONL commit; missing JSONL = unverified cycle. |
| T-08-04 | Tampering | Operator passes 5/5 but ignores R-9 manual E-Stop test | mitigate | Acceptance row 4 explicitly requires E-Stop injection; operator must report verdict. |
| T-08-05 | Spoofing | Pi5 deployment uses sim_lidar_docking.launch.py instead of production launch | mitigate | Launch file docstring + Plan 02-04 navigation.launch.py are clearly separate; CI deploy uses navigation.launch.py exclusively. |
</threat_model>

<verification>
- `colcon build --packages-select mowgli_simulation` exits 0
- `colcon test --pytest-args -k synthetic_scan` reports >= 1 test passing
- e2e_test.py has _run_fine_dock_phase method
- mow_session_monitor.py logs /dock_match/* + lateral_error_at_contact
- 02-VERIFICATION.md has R-1..R-13 + E2E + hardware rows
- 02-08-PI5-CHECKLIST.md is operator-readable
- Pi5 5-of-5 result file (02-08-PI5-RESULTS.md) exists with operator-reported verdicts (gates Phase 2 closure)
</verification>

<success_criteria>
- D-14 satisfied: synthetic_scan_kicp_publisher in mowgli_simulation; sim_lidar_docking.launch.py composes the pipeline; e2e_test pre-validates R-7 in CI
- D-15 satisfied: mow_session_monitor extended with /dock_match/* fields + lateral_error_at_contact + summary aggregates
- 02-VERIFICATION.md tracks all 13 R-XX requirements + E2E + hardware row
- 02-08-PI5-CHECKLIST.md prepares the operator-gated 5-of-5 hardware run
- SPEC R-7 hardware gate: 5-of-5 first-attempt successes with ≤2cm lateral / ≤1° yaw at contact (operator-verified)
- SPEC R-9 hardware gate: E-Stop during FineDock + no auto-restart after ResetEmergency (operator-verified)
- SPEC R-11 hardware gate: at least one cycle demonstrates auto-refresh fired with inlier ≥ 0.95 (operator-verified)
- Phase 2 close pattern matches Phase 1: automatable scope COMPLETE; operator-gated hardware checkpoint pending until 02-08-PI5-RESULTS.md is committed
</success_criteria>

<output>
After completion of Tasks 1+2 (Task 3 is operator-gated), create `.planning/phases/02-lidar-dock-pose-estimation/02-08-SUMMARY.md` describing: synthetic publisher API + dock geometry, sim_lidar_docking launch composition, the e2e_test phase + assertions, the mow_session_monitor schema additions, the VERIFICATION.md matrix state (UNVERIFIED -> VERIFIED transitions await /gsd-verify-phase 2), and a clear "Phase 2 status: automatable scope COMPLETE; T3 hardware checkpoint pending operator verification" sentence. The phase-level SUMMARY (created by the orchestrator after this plan) gates on operator returning the 5-of-5 verdict in 02-08-PI5-RESULTS.md.
</output>
